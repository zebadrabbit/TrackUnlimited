[/Script/EngineSettings.GameMapsSettings]
; WHICH MAP A PACKAGED BUILD OPENS, and there was no answer here at all.
; Unset, the engine falls back to its own empty default map -- which contains no
; ATUCoasterRide, so BeginPlay never runs, the shell never boots and -TUSmokeTest
; tests nothing while exiting 0. Found by writing the packaged smoke test rather
; than by packaging: nothing about it is visible from the editor, where the map
; you have open is the map you get.
GameDefaultMap=/Game/Maps/VerticalSlice.VerticalSlice
EditorStartupMap=/Game/Maps/VerticalSlice.VerticalSlice

[/Script/Engine.RendererSettings]
r.DynamicGlobalIlluminationMethod=1
r.ReflectionMethod=1
r.Nanite.ProjectEnabled=True
r.DefaultFeature.MotionBlur=False

