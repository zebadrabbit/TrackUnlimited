# CLAUDE.md

Instructions for Claude Code (or any other AI coding agent) working in this repository. Read this before writing or changing anything.

## What this project is

TrackUnlimited: a free, open-source, Unreal Engine 5 roller coaster simulator built as a successor to NoLimits 2 — engineering-grade physics and track precision, a modern approachable UI, and a real block-signaling/ride-control system neither NoLimits 2 nor Planet Coaster has. Solo-developer-led. PC flatscreen first; VR is a deliberate later phase.

## Read first

- `Docs/PROJECT_PLAN.md` — the full plan: vision, market context, all five product pillars, MVP/full scope, technical architecture, phased roadmap, solo-dev execution strategy, legal/licensing, risks, and immediate next steps. Read this in full before making any architectural decision — most "why isn't this built like X" questions are already answered there.
- `Docs/PHASE0_FINDINGS.md` — what the prototypes actually proved, with numbers, and the known-limitations ledger. Read this before changing either prototype header. Several plausible-looking "fixes" have already been tested and shown to make things worse; that page says which, and why.
- Project board (Trello): https://trello.com/b/Uzqm38o7/coaster-sim-nolimits-successor — the live, day-to-day source of truth for what's in progress/done. This repo's docs are the design reference; Trello is where task status actually lives and gets moved around. If you have Trello MCP access in this session, check it for current status before assuming a task list from the docs is up to date.

## Non-negotiable architectural constraints

These were deliberate decisions made after discussion, not defaults — do not "helpfully" change them without raising it first.

1. **No viewport click-drag-place track editing, ever.** Track segments are numeric/formula-driven parametric data (straight, constant-radius curve, clothoid transition, helix), each defined by typed values/expressions. The 3D view is a read-only preview, not the editing surface. This was an explicit rejection of the Coaster Forge / Satisfactory-style direct-manipulation model — do not add drag handles "for convenience" or "for a quick prototype."
2. **C++ for physics and signaling.** Blueprint is fine for editor tooling, UI, and rapid iteration on non-critical systems, but the energy-based motion model (Section 5, "Physics simulation") and the block-signaling state machine (Section 5, "Signaling and ride control") must be C++.
3. **No dependency on Train and Rail System (Polygon Jelly) or any other paid marketplace asset in committed/shipped code.** It's reference material the developer used while prototyping locally — never to be redistributed or have its code/assets copied into this repo.
4. **No Unreal Engine source in this repo, ever.** Project code and content only, per the UE EULA. Contributors get the engine themselves via Epic Games Launcher or their own linked GitHub access.
5. **No real manufacturer trademarks or exact ride designs** (Vekoma, Intamin, B&M, RMC, etc.) without explicit permission. Use original/generic naming and designs.
6. **MIT license** on everything committed here (see `LICENSE`).
7. **Three tiers, and requests never travel down. DECIDED 2026-08-06.** Safety is C++ and is **not scriptable, not overridable, and has no debug bypass that ships** — the E-stop chain, restraint proof, gate interlocks, overspeed, anti-rollback, block veto. Control (dispatch policy, sequencing, device commands) and Show (lights, audio, effects, cameras) sit above it and may **ask** the tier below for something and be refused; they may never grant themselves anything. This is not invented: Alcorn McBride state in their own manual that show equipment "is not intended for use in applications where a malfunction can reasonably be expected to result in personal injury", and nothing that can hurt a guest fires from a show controller alone. **It answers structurally, rather than by policing, the question every user-scripting system faces — can a downloaded track make the ride dangerous?** No, and not because scripts are validated well: because the layer that could is not scriptable, for the same reason it isn't in a real park. **SHOW IS READ-ONLY** and makes no requests at all — it subscribes to the state-transition stream and writes to fixtures, which is also how DMX512 works on the wire. Hazardous effects are gated by a hard permissive they cannot see or influence: a show controller fires into a circuit that may be open and does not know. **TIER 2 GETS EXPRESSIONS, NOT STRUCTURED TEXT. DECIDED 2026-08-06.** An override is a pure expression over a named process image — no statements, no assignment, no loops, no state across scans — evaluated against the snapshot the scan already takes. Written in **strict ST spelling from day one** (`AND`/`OR`/`NOT`, `=` for equality, `<>` for inequality, `SEL(cond, false, true)` rather than a ternary) so it is a genuine subset and a later ST is an *extension* rather than a replacement. Roughly 900 lines, and **the process image is the larger half of it** — Tier 2 needs that whichever language wins, so none of the work is speculative. **The safety rule is structural, not policy: an override may only make a permissive MORE restrictive.** It is an extra `AND` term on the existing chain, so there is no syntax for loosening one and no review needed to be sure — which is what makes an override from a downloaded track safe by construction. What it cannot express is **state and sequencing**: *"run forward 4 s, then reverse, then park backwards"* needs variables that persist across scans, and the day something wants that is the trigger for revisiting ST. Nothing built or planned needs it — shuttles, turntables and transfer tracks are all unbuilt or parked — and the C++ default program has absorbed every ride type attempted so far, including the small-batch platform, which turned out to be mostly emergent. **TIER 2 IS BUILT (2026-08-06):** `PlcImage.h` is the process image — 73 named slots on the two-train circuit, **declared from the layout** so an override written for another track fails to BIND, and **generated rather than maintained** (`Describe()` enumerates every slot, and the suite asserts each one binds back to itself, so autocomplete and the docs cannot drift). `PlcExpr.h` is the lexer, precedence-climbing parser and pure evaluator. **`Restrict()` is the only entry point and it is one `&&`** — five deliberately hostile always-TRUE expressions are asserted to grant nothing. A **broken override leaves the permissive UNCHANGED** rather than denying, or a typo in a downloaded track is a denial of service. Types are checked at parse time, and the C spellings (`!=`, `&&`, `? :`) are REFUSED — which is what keeps it a subset rather than a dialect. `Docs/TIER2_OVERRIDES.md` is the reference and **every example in it is parsed and bound in the assert suite**, so a stale one fails the build rather than somebody's evening. **Tier 3's language stays open** and is answerable once cues exist; `FShowBus` is the boundary and needs no language at all.

## The control layer is a LAYER OF CHOICE, and an on-ramp

Settled 2026-08-06, and it governs every UI decision the shell work is about to make.

**Nobody should ever have to open the control layer to have a working, exciting ride.** Every preset runs on the generated C++ default: place track, it works, trains dispatch, blocks interlock. Tuning is numbers in a panel. An expression override is for the case the default will not do what somebody wants. Nothing above that is ever required.

**So the risk is presentation, not architecture.** A `PROGRAM` tab in the main navigation says "you need this". A first-run experience showing a controlword says "this is for engineers". The depth alienates nobody as long as it is *findable by someone who wants it and invisible to someone who does not* — which is the same progressive-disclosure rule already applied twice, in `EditConditionHides` and in the operator/maintenance panel split.

**And the depth is meant to be worth learning.** The developer's framing, and it is the better one: *it should spark interest that could lead to a career decision in the real world.* Industrial control is a real, understaffed, well-paid field that almost nobody discovers as a teenager, and a coaster is a far better first PLC than a bottling line.

**That is a second and independent argument for the verbatim-standards choices** — CiA 402, IEC 61131-3 spelling, IEC 60204-1 stop categories, the enable handshake. If this taught an invented control language, learning it would be worth nothing outside. Because it is the real interface, what somebody learns here is what they would use in industry. **Fidelity is what makes the on-ramp lead somewhere**, so "close enough" is a worse answer here than it looks.

Evidence the shape works: NL2 has a scripting API most of its users never touch, and it is a ceiling rather than a floor — the professionals who prototype on it are why it has credibility, the hobbyists who never open a script are its volume. And the strategic point underneath: a project that tries to be easy-first competes with Planet Coaster and loses. The stated position is the NL2 gap.

## Current phase: Phase 4 — Track Meshing & Supports (geometry done, art and shell remain)

**Phases 0, 1, 2 and 3 are complete** (Phase 2 closed 2026-08-06 when drag was finally measured). Phase 3.75 is mostly built or deliberately deferred with the reason on its card. **PHASE 3.5 IS SUBSTANTIALLY DONE as of 2026-08-08** — the shell boots, the menu opens tracks, settings persist, the document saves and loads with its control layer intact, undo works, the segment editor inserts and removes and multi-selects, and the console is a control board you press. **AND AS OF 2026-08-21 IT IS DONE** — the three items this sentence called "genuinely unstarted" all shipped, and the sentence outlived every one of them, which is this file's recurring failure and the reason to date a claim. The packaged build and its smoke test landed 2026-08-16 (`cae74a3`); **plan-view thumbnails** and **save-as with a typed name** landed 2026-08-21. The thumbnail is worth knowing about because the deferral that stood in `TrackBrowser.h` had the wrong premise: it said a thumbnail wants a render on save, which is exactly what makes one expensive — a picture taken at one moment, stale as soon as the track changes, absent for anything written by a build that did not take one. **A track already carries its own picture**: `PlanThumb` is the plan view, taken from the walk the browser ALREADY does to read length and height, so it costs nothing, cannot go stale, needs no file, and exists for a track nobody has opened. Aspect ratio is preserved because **a browser whose pictures lie is worse than one with none**, and a dead-straight track — zero extent across, the commonest first track in the world — is drawn down the middle of its own axis rather than being the one row with no picture. Save-as is **Shift+K and deliberately NOT a second row in the settings table**, because nothing is bound to a chord and `CheckBindingsAgainstInput` would correctly call that row a stale promise.

**Why Phase 4, and what is actually missing from a screenshot now.** The original argument was that the control system is deeper than anything on the market and **completely invisible in a picture** — and for a free open-source project wanting contributors, what recruits them is a coaster rather than a paragraph about CiA 402. That still holds; the specifics have moved.

**THE TRACK IS NO LONGER WIREFRAME. That sentence stood here after it stopped being true** — `RebuildTrackMesh` is wired, on by default, and pushes rails, spine and ties as three procedural mesh sections. **AND SO ARE THE SUPPORTS AND THE CATWALKS** (2026-08-08) — 62 legs on 134 footings on the vertical slice, decks and handrails on every powered run. The paragraph that stood here said `PlanSupports` computed legs and threw them away; that stopped being true the same week, which is this file's own recurring failure and the reason to date a claim rather than assert it. **AND THE TRAIN IS BUILT (2026-08-20)** — `Prototypes/TrackMesh/TrainMesh.h`, engine-free and assert-tested like everything else here, ported behind `bBuildTrainMesh` with the old cubes one click away. `Cars` was one `/Engine/BasicShapes/Cube` per physics sample point, so the payoff for the physics, the signalling, the envelope and the meshing was a stack of grey boxes — and **a sample point is not a car**: nine is what the mean-height gravity integration needs, where `CarCount × CarLengthM` is what somebody authored. **The sample points keep doing physics and stop doing rendering**, and the two lists are allowed to differ and always will. Four buffers because four materials: lofted shell, chassis beam, twelve wheels a car in **three sets gripping one rail from three directions**, and the couplers. **ARTICULATION COST NOTHING** — each car takes its frame from its OWN arc length, so a rigid body chords across an arc while the wheels stay on it; asserted against `sqrt(R² + h²) — R` rather than the chord sagitta, which is a different number (0.0562 against 0.0563 on a 20 m radius) and is close enough to have looked right. **THE FLOOR TAPERS, AND THAT IS NOT STYLING**: the body line runs through the rail plane, so a full-width floor is drawn through both running rails and buries the wheels — which are the entire reason it reads as a coaster rather than a bus. **AND THE FIRST TAPER WAS STILL THROUGH TWO OF THEM (fixed 2026-08-21).** One straight flare from the floor crossed both wheel bands on the way up: the running wheel by 0.168 m, reported from a screenshot, and **the side-friction wheel by 0.167 m, which no screenshot can ever show** — side friction runs against the rail's INNER face, so it is a puck lying flat at exactly floor height, inside the body. Fixing what was on screen would have left it. `ShellKeepOut` now derives the envelope FROM THE WHEELS, so the flank has **two knees rather than one flare** (a narrow pan between the side pucks, out over the running wheels, full width at the shoulder) — which is also the shape a real car has, put there by the same wheels. **The suite had the gap and its own comment gave it away**: it said the wheels were the reason and then measured one height, the floor line, against the RAILS. A wheel is a BAND of heights, and the widest the shell gets inside that band falls between section vertices rather than on one. Clearance is asserted across nine gauge × wheel-size combinations, because three hand-tuned numbers would clear THESE wheels and go on clearing these wheels after somebody changed a diameter. **0.9 m is still a ceiling and not a guess**, reported never repaired. Per frame it UPDATES vertices rather than recreating a section, because recreating one rebuilds a render proxy and at frame rate that is the whole cost of the feature. Measured in `-TUSmokeTest`: **5 cars of 3.0 m, 688 triangles a car, the tub encloses 1.428 m³ of shell, silent** (616 and 1.377 before the bulkheads, 568 and 2.669 while it was a box). It was homeless on the board until 2026-08-19, having been deferred on 2026-08-02 to a phase where the card was never written. **Out of scope and staying so**: a nose, wheel spin, suspension travel, a second style — one credible vehicle was the gate. **Restraint geometry came off that list on 2026-08-27** — see the lap-bar entry under the airgates below.

**COASTER TYPES ARE DESIGNED, not scheduled** — `Docs/COASTER_TYPES.md`. The industry taxonomy is a MARKETING taxonomy: it sorts by what a rider notices, which is the right list for a brochure and the wrong one for a simulator, because two entries that read as equally different can be a field and a rewrite. Sorted by cost instead it falls into three piles. **Already expressible**: every launch mechanism (`Lift`/`Launch` plus `ZoneAccel`/`ZoneSpeed` — the honest gap is the CURVE, since a hydraulic pull, a stepped LSM and a fading pneumatic are one constant each today, and a rider can tell them apart with their eyes shut), and most of "by layout" — dive is a holding brake at a crest, wild mouse is tight radii and short cars. **One parameter away**: track structure is `FTUTrackStyle` plus a roughness term that belongs to the TRACK rather than the type; and **seating is mostly an offset, because the heartline is already the rider** — inverted is `SpineDropM` negative, flying is that plus a prone seat, and `FacingSign` already exists. **The wing coaster is the one worth building properly**: a rider offset laterally feels roll-rate × offset that the centre seat never does, it is measurable with what `GEnvelope` already has, and **it applies to the outer seats of every wide train**, so it is a general improvement a wing coaster merely needs most. **Genuinely new — a degree of freedom each**: spinning (car yaw, do this one first), suspended (a damped pendulum), 4D (a second orientation frame everything measuring G must learn), bobsled (no lateral constraint — barely the same simulation). **AND THE FIRST ONE IS BUILT (2026-08-29): `ETUPresetLayout::Wing`.** The Sidewinder's track **byte for byte** with a different vehicle on it, which is the argument shipped rather than argued. The vehicle is **two numbers** — `SeatPitchM` 2.80 (the two seats of a row, 1.4 m either side) and `PodWidthM` 1.30 (a shell built around each seat instead of one spanning both) —  **NARROWED THE SAME EVENING, FROM A SCREENSHOT.** The first try was 3.60 and 1.10 — 4.70 m of vehicle either side of a 1.20 m gauge to carry TWO people, which read as two thin troughs on the end of a lot of scaffolding rather than as a car. **A real wing coaster is that wide because each wing carries two seats abreast; ours carries one**, so it had no business being the same span. 2.80 and 1.30 is 4.10 m overall, the pods 18% wider and 0.40 m closer in each side with the spars that much shorter, and a pod 1.30 across by 2.70 long for its two rows is a tandem pod rather than a rail. `CarColumns` turns the two numbers into one shell or two, so the seats, the bulkheads and the lap bars are built per COLUMN and a wing car is the same code run twice with a lateral offset rather than a second train style. **NO SECOND SOURCE FOR WHERE A RIDER IS**: the pod is built AROUND the seat, so the camera, the felt-G offset and the shell all read `SeatPitchM`. **Nothing downstream learned that types exist** — the envelope judge already asked at `SeatPitchM * 0.5` and picked the offset up with no change at all, which is the thesis working rather than a coincidence. **A pod is not over the wheels**, so its flank is unconstrained and it is full width to its floor; **AND "A POD IS NOT OVER THE WHEELS" WAS HALF-APPLIED, WHICH SHOWED.** It was honoured for the two flank knees and not for the taper or the floor, so a pod kept the TUB's shape — and the tub's taper is not taste, it is the wheels: it narrows at the bottom to get past the running and side-friction sets and reaches full width only once it is clear of them. A pod inherited that and with it a cabin floor **0.47 m up a 0.90 m shell**, leaving 0.135 m of pod above the squab and a rider **perched on the rim** rather than sitting down in it (reported from a screenshot; the rule was already written and obeyed twice out of four times). A pod reaches full width immediately above its bottom corner and its floor sits at **0.21 m**, so there is 0.39 m of shell above the squab and the eye — derived from the seat, never authored — comes down with it to 0.36 m over the rim. Asserted against the shell rather than as figures, so a taste change cannot re-perch anybody. three ways it goes wrong are audited (narrower than its seat, pods through each other, a pod through the bogie) and the good one and the ordinary train are asserted SILENT. **Every pod lands on something** — spars from the chassis at each bogie, the supports' lesson one vehicle down. Asserted in `-TUSmokeTest` **both ways**, because the sameness is half the test: the layout must be identical and the seat must read differently — and the first version confused *the layout* with *the ride*, failing because a five-car wing train has a different top speed to a seven-car one, which is exactly what should differ. **MEASURED:** the wing rides the Sidewinder's 65 segments and 1284.8 m unchanged, on pods at ±1.40 m, and the envelope judge finds **one thing at the wing seat against none at the heartline** — the snake is within the bands for a rider on the centreline and outside them for one on a wing, which is a fact about that ride nothing could see before the offset term existed. Report, never repair: it is a row to go and look at. **One honest gap it opened**: a wing train occupies the space a trackside catwalk needs (a deck starts 0.85 m out, its handrail tops out 0.30 m under the heartline — inside the pods laterally *and* vertically), so the preset authors **no walkway** and says so rather than drawing a route the train runs through every lap. **THE DECISION: a type is a PRESET, never a branch.** No `ECoasterType` the physics or signalling reads — the moment a type forks behaviour every later feature costs N times, which is NL2's fatal scripted-mode choice one level down. A type selects values in fields that already exist, so nothing downstream ever learns types exist, and a type nobody anticipated is a set of values somebody types rather than a pull request. **Alpine/rider-braked is a CONTROL question, not a vehicle one** — the only entry where the rider has an input, which is a fifth party alongside safety, control, show and the operator.

**THE TRAIN WAS DESIGNED FIRST AND IS NOW BUILT** — `Docs/TRAIN_DESIGN.md` is still the spec and still worth reading before changing a dimension, because every number in it already existed somewhere else. **PROCEDURAL, for the same four reasons everything else here is**: an imported model is the only binary art asset in an MIT repo, it needs a provenance check generated geometry does not, it is the one object a contributor cannot read a diff of, and there is no artist. **A coaster train is not a railway train and the difference is the WHEELS** — running, side friction and upstop, three sets gripping one rail, and the upstops are why a train survives −0.94 g instead of leaving the track; from the chase camera that assembly is more of the silhouette than the fibreglass is. **Articulation comes free** from giving each car its own arc length, and is the motion nobody manages to fake. **AND THE CAMERA DOES NOT WANT A BONE, IT WANTS A SEAT** — a socket on the mesh is a second source of truth for where the rider is, where felt G is computed at the heartline, and the whole reason that model exists is that a rider's eye and a rider's G are the same point. `FSeat` is data and the frame is derived, which is one answer for the camera, the mesh and `GEnvelope` — and `LateralM` is exactly the wing-coaster prerequisite the types doc asks for, so three cards want it rather than one.

**Turnouts and reverse running are DESIGNED, not scheduled** — `Docs/DIRECTION_AND_ROUTES.md`. Two rides prompted it and the developer's framing is the right one: Big Thunder Mountain and Expedition Everest are **casual concepts wearing a mask**, a track merge and a reverse section, and writing either as a ride feature would be the mistake. Three things in it are worth knowing before touching the signalling or the envelope. **Direction is TWO signs, not one** — travel and orientation are independent, and the proposal keeps arc length fixed (a survey does not change because a train is going the other way) while every derived rule asks for `LeadingEdge = Orientation × Travel` rather than assuming a nose. **The sharpest consequence is not in the signalling**: `GEnvelope`'s fore-aft bands are asymmetric 3:1 (`+Gx` 6 g with a headrest, `−Gx` 2 g), so a backwards train has both signs swapped in the rider's frame and is judged against the wrong band — it will pass an unsafe event and report "within envelope" while doing it. And **facing vs trailing is a property of the ROUTE, not the point**: on Everest the same physical point is trailing on the way up and facing on the way back, which is why direction has to land before routes rather than beside them. **Explicitly out of scope**: an elevator-style moving launch platform — a mechanism rather than a topology, and the rule it instances is *rare mechanisms wait; general capabilities do not*.

**Deliberate deferrals, not unfinished work.** Phase 1: ride-profile trace legibility. **The loop side-step is NO LONGER one of them (2026-08-26)** — it stood here as "the authored vocabulary cannot express a rideable one, five fixes measured and rejected", and the sixth is a new authored quantity rather than a tuning: `TorsionRatio`, torsion as a constant multiple of curvature, which by Lancret's theorem makes the eased loop a generalised helix that side-steps without translating (legs 0.189 → 3.46 m on the reference layout, every ride figure unchanged to the second decimal). Its two companions matter as much: `ERollMode::FollowsTorsion` (the rider's up follows the bend, or the twist reads as lateral G) and `InTwistedFrame` (the path frame leaves a torsioned loop twisted by 2π·cos ρ, and a turn typed as plain yaw in that frame pitches by sin(twist) of its turning — 24 m of drop, measured). Read the RESOLVED entry in `PHASE0_FINDINGS.md` before touching a loop. Phase 3: turnouts and block topology beyond a closed circuit, parked by the developer as a far-future stretch goal. Phase 3.75: everything script-shaped, waiting on nothing more than a need.

**Do not start a large new subsystem without checking Trello first** — several things that look missing are done, and several that look open are parked on purpose.

What exists now, all of it engine-free and assert-tested under `Prototypes/`, with a thin UE actor over the top:

- **Track geometry** — curvature profile over arc length; straight, arc, clothoid, helix (via constant torsion), a generalised helix via `TorsionRatio` (torsion proportional to curvature — the side-stepping loop), and **`Pitch` (2026-08-26)**: a vertical curve from what somebody types — pitch change, tightest radius, ease in / ease out / constant, length derived. **Until then no hill had been authored outside a preset**: every crest was two Raw segments carrying derived lengths and curvatures, and Raw is not a kind the runtime editor cycles into, so the shipping path could turn but never climb. The reference layout's hills are Pitch rows now (bit-identical to the Raw pairs, asserted); the circuit presets keep their Raw pairs, because their closure was solved against float-rounded lengths and a second rounding is a seam. `TrackSpline.h`
- **Authored data model and diffable JSON** — stores what was typed, never what was derived. `TrackIO.h`. **THE FORMAT CARRIES THE CONTROL LAYER, AND UNTIL 2026-08-08 IT DID NOT** — `FAuthoredSegment` was geometry only, so a saved track was a ride with no brakes, no station and no lift, and nothing could see it because *the ride is the same shape either way*. It now carries `Zone` (by **stable name**, never an index — a renumbered enumerator turns every stored ride into something else), `ZoneSpeed`/`ZoneAccel`/`ZoneDecel`/`ZoneBrakeDecel`, `bAntiRollback` and `bStartsNewDevice`. Three rules fell out. **A field is omitted only when at its DEFAULT, never because the zone is None** — the editor hides a device's speed on plain track and *hidden is not deleted*, so a writer that dropped it would undo that rule at the one moment it costs work. **An unknown device name is REFUSED**, exactly as an unknown kind already was, because reading it as plain track opens somebody's ride with a brake missing from the middle of it. And **a flag is `true` or it is absent** — `false` is never written, so anything else is refused rather than read as off, which for a catch is the one direction it must not fail in. Old files still load: every field is optional, so no version bump. **`ToAuthored` now has an inverse**, which is not a reversal of the one-direction rule — that rule is about `BuildSegment`, where deriving a helix back out of an integrated curvature profile is the problem the authored model exists to avoid. These two are authored-to-authored and the pair must be lossless, because it is what a save and an open go through.
- **AND THE BRIDGE IS CHECKED ON EVERY LAUNCH** — `ATUCoasterRide::CheckDocumentRoundTrip`, at BeginPlay. Save, open, save again, and the two texts must be identical; on the vertical slice that is **33 segments, unchanged**. It exists because the editor-struct half of the save path is the one part with **no engine-free test** — `FromAuthored` returns a UPROPERTY struct the reflection system owns — so the check runs on the real preset where it costs nothing. Same shape as the sensor layer's second means of detection: two routes to one answer, disagreement reported rather than resolved. Measured: dropping one field from `FromAuthored` produces `OPENING AND RE-SAVING THIS TRACK WOULD CHANGE IT`. What it **cannot** see is a field `ToAuthored` never carried — then the file never had it and both texts agree; the guard for that is the `sizeof(FAuthoredSegment)` static_assert in `test_trackspline.cpp`, which fires when the struct changes and names the four places to add it.
- **The document is OBSERVED, so dirty is real** — `RebuildFromSegments` hands the session the serialised text, which is where every edit ends. Before this nobody called `Observe`, so `IsDirty()` was false for ever: the frame's unsaved marker could never appear and `TickAutosave` returned early on every call. **Autosave is still deliberately NOT wired** — it writes a sidecar for crash recovery to offer at Boot, and an actor placed in a level has already skipped Boot because *the level is the document*. The sidecar gets a consumer when documents have paths, and not before.
- **SAVE AND OPEN WORK, AND THE MENU IS REACHABLE** — `SaveDocument`/`SaveDocumentTo`/`OpenDocumentFrom`, `Saved/Tracks/*.track`, `[K]` saves and `[M]` goes back to the menu. **No name prompt, and that is a deferral rather than a design**: naming needs *text* entry and the segment editor's typed entry is numeric, so an unnamed save picks the first `Untitled-N` that does not exist — **never clobbering**, because silently overwriting the last unnamed track is the one outcome a save button must not have. **NOT Ctrl+S**, which is the F1 lesson again: the editor binds it to Save Current Level and its bindings are live in PIE, so the familiar chord would write two different documents from one press. **Paths are made ABSOLUTE before they are stored** — `ProjectSavedDir` is relative to wherever the process was launched, and a recent list of `../../../../` entries is one launched-from-elsewhere away from every row reading as missing. **UTF-8 is FORCED on write**: `SaveStringToFile` defaults to ANSI when the text happens to fit and UTF-16 when it does not, so one non-ASCII character silently changes the encoding under a loader that reads UTF-8. **An open is marked clean against the CANONICAL text, not the bytes read** — `IsDirty` compares against a re-serialisation, so storing the file's literal bytes would report a hand-edited-but-equivalent file as modified the moment it opened; the stated consequence is that saving normalises formatting. **Opening lands in RIDE**, per the program-flow rule that the action picks the mode.
- **AND THE ONE TRANSITION THAT ASKS NOW ASKS** — `bConfirmingMenu`, drawn last so it is over what it is asking about, and it rebuilds the click list so a click cannot fall through to the thing underneath. Three answers, and **save is first while discard is not defaulted**; **"save and leave" only leaves if the save SUCCEEDED**, because discarding work after failing to preserve it is the worst available order. Build↔Operate↔Ride still never asks. This branch existed and was **dead code that looked alive** until the session was told what the document is.
- **THE MENU LISTS THE FOLDER, NOT JUST THE RECENTS — and every recent row used to claim it was missing.** `FTrackBrowser::Rows(Known, Paths)` marks anything it has no entry for as MISSING, which is correct for a file that cannot be read and wrong for one nothing looked at; the caller passed an **empty** `Known`, so a present, readable, recently-opened track was labelled *"not found — the drive or folder may be disconnected"*. `RefreshTrackList` now reads and parses each candidate: recents first (most-recent-first is the order somebody is looking for), then anything else in `Saved/Tracks`, deduplicated — which is what makes a track saved on another machine, or by an install whose recent list is gone, findable at all. Length and height come from **one forward walk** (`WalkTrack`), never a sample loop, because `EvaluateAt` is O(track length) per call and that is the O(n²) trap this project keeps unlearning. Cached on the way into the menu, because `DrawMainMenu` is immediate-mode and parsing every file at frame rate is a cost that stays invisible until somebody has thirty tracks. All four row states are exercised by `-TUSmokeTest`: two that load (`1288.0 m, 48.5 m tall`), one carrying the parser's verbatim reason, one missing.
- **`-TUSmokeTest`** boots, writes a real file, reads it back and proves the ride is the same one — the first half of the packaged smoke test the build-script card is waiting on. Measured: **33 segments saved, opened and unchanged, session clean**. Off unless asked for.
- **Validation** — report, never repair. `TrackValidate.h`
- **Circuit closure solver** — damped Gauss-Newton over authored parameters. `TrackClose.h`. **AND THE SEAM NAMES ITS LEVER (2026-08-26)** — `SuggestClosureLever` is the hand-run probe that found the Sidewinder's closure, as a diagnostics row: perturb each length-bearing segment by a metre, see where the end goes, and name the one change that closes the most of the gap — *"segment 26 (straight, 24.0 m) moves the seam 1.00 m per metre along (+1.00, −0.00, −0.00): lengthen it 5.0 m and the gap closes to 0.00 m"* — or, when no single length can, say so and that **the next lever needs a segment running a different way**, which is the lesson itself: every +X lever on the Sidewinder pinned at a floor while the seam sat 1.7 m short, because the one −X length had never been freed. Only asked of a near miss (plan gap under 50 m, ≤ 200 segments); a point-to-point ride ends far from its station on purpose and a CSV import is four thousand candidates for a question nobody asked.
- **Undo/redo** — snapshots, with the save format as identity. `TrackHistory.h`. **AND THE RUNTIME EDITOR CAN ADD AND REMOVE SEGMENTS NOW** — until 2026-08-08 it could change numbers on segments that already existed and nothing else, which is a tuning panel rather than an editor; `Insert`/`Remove`/`Duplicate` had been in the model since Phase 0. `[I]` inserts and `[R]` removes. **Insert COPIES the selection rather than inserting a blank**, because building track is almost always *another one like that* — the new piece inherits the kind, the roll and the zone somebody was about to retype — and a default straight is what you get only when there is nothing to copy, which is the empty track and the first segment. **Remove is UNCONFIRMED, which is only defensible because undo landed first**: a confirm on every delete is the dialog people learn to click through, and a working undo is the honest alternative rather than a prompt standing in for one. These two are the only letters that guard on `IsTypingInField` — not because a letter could be part of a number, but because they change the LIST somebody is typing into. **WIRED 2026-08-08, and it had never been constructed once**: the Details panel has Unreal's transactions, so the *developer* path always had undo and the shipping one — the runtime segment editor — had none at all. `[J]` and `[L]`, either side of `[K]` save; **not Ctrl+Z**, for the same reason `[K]` is not Ctrl+S, and worse here — the editor's undo is live in PIE, so one press would step two histories back, one of them through the level. **A rebuild is NOT an edit**, so nothing commits from `RebuildFromSegments` however tidy it would look beside `Observe`: a rebuild also happens for a preset, an open and an undo, and committing there makes the second press go forward again. `PushHistory` is called from the two places an edit FINISHES. **A merge key in the Details panel and none in the runtime editor** — that panel is the control that fires continuously (a spinner drag is twenty callbacks and one edit), where the runtime editor already commits on Enter and never per keystroke, so its coalescing happened before history ever saw it. **A new document is a new history**, because undoing across an open would restore another track's geometry into this one. Measured: an edit undoes and redoes **exactly**, compared on the document TEXT rather than a segment count — breaking the apply reports `undo/redo did not round-trip (changed 1, back 0, forward 0)`.
- **Train physics and the ride profile** — the whole ride measured at edit time. `TrainPhysics/`
- **Block signalling, wired to the ride, for N trains** — the state machine in `BlockSignal/BlockSignal.h`, and `BlockSignal/RideSignals.h` mapping each train's nose-and-tail arc length onto it. `RideSignals` takes **doubles and a train index, not an `FTrain`**; keep it that way, it is why the whole layer is testable without the physics. The actor derives blocks from the contiguous-zone walk it already did. Three reads carry train identity — the entry test, the exit release, and the permissive — and each one is what stopped a specific silent fail-open; `Tick` is **once per frame, not once per train**. Holding is done by **gating the zone**, never by declining to integrate.
- **Block brakes that actually hold and release** — `FTrain::SetZoneTargetSpeed` retargets a zone at runtime and `FindHoldZoneAt` says where a train may be parked: a device needs **both** authorities, so a trim brake (cannot start a train) and a launch (cannot stop one) are both refused. Whether it can stop the train in the block it has is a separate, layout-only question — `v²/2a` against the block length.
- **A speed trap** — `FSpeedTraps` in `TrackSensors.h`. Two switches a **surveyed** distance apart and a clock; speed is the gap over the time between rising edges, no position and no identity. The gap is read **once at commissioning**, the same idiom as the stop mark consuming train length at survey. Quantisation is a real design constraint and is reported: at 240 Hz a 1 m trap on a 30 m/s train is an 8-tick, **12.5%** bound where a 10 m trap is 1.25% — and what is asserted is the *bound*, because realised error is not monotonic in gap width. **It never trips on the real circuit, and that is the result**: what it buys is the **stopping margin**, which falls from 13.7 m healthy to 5.1 m with one brake dead. A margin degrades continuously where a trip is a cliff.
- **Sensors, and a train counter over them** — `BlockSignal/TrackSensors.h`. `FRideSignals` is HANDED each train's exact span every frame, which is a cheat: a real PLC knows a switch went high and nothing else. `FTrackSensors` is the only layer entitled to a position, reports presence WITHOUT identity (a switch says "metal is over me"), and `FBlockCounter` derives occupancy as *entries at sensor i minus full clearances at sensor i+1* — rising in, FALLING out, which is what stops a block going clear under a straddling train. Proven equal to perfect knowledge over three laps. **A counter must be SEEDED**; lifetime totals send the block behind a train's start to −1. Two kinds of switch exist so far: **block-boundary sensors** (occupancy) and **stop marks** (one per zone, where a trucking train stops). Inputs are scanned **once at the top of the frame** — read inputs, execute, write outputs, as IEC 61131-3 does — so every train is served against the same snapshot. **The counter now runs ALONGSIDE the interlocking as a second, independent means of detection, and a disagreement trips the E-stop** — same blocks, same frame, nothing in common but the answer. That is what a real installation buys with a second detection method rather than a better single one, and it is what makes the sensor layer load-bearing instead of decorative. Also trips on a block counted twice (a collision derived from switches alone) and on one counted below zero (a *lie*: missed trip or bad seed). **Circuits only** — `FBlockCounter` is a counter over a ring, and on an open layout the wrap is false. Measured: both circuits, every train count, agreement on every block on every frame; mutating the falling-edge rule fails it, so the check bites. What still cheats is in `SIGNALLING.md` § "What the system actually knows": the interlocking's own occupancy is still the span (flipping which is primary needs train *identity* inferred from trip order — a rewrite of the safety core, not an addition), and `ServeHolds` still asks `FindHoldZoneAt(GetDistance())`.
- **A station is a PROCESS, not a place** — `BlockSignal/StationProcess.h`. Arrive → unload → load → restraints → all-clear → dispatch, and **the dispatch permissive is now an AND**: blocks clear *and* every station gate. Each gate is a **contact, not a timer** (`FAutoStationCrew` asserts them on dwell timers only because nothing simulates riders, and it is a separate object so it can be deleted). **Readiness is continuous, the release is LATCHED** — those look contradictory and both are load-bearing: a restraint opening in `Ready` drops the permission the same frame, but omitting `Departing` from the permissive **deadlocked the circuit** (released train rolls off its mark → not "in position" → permissive drops → re-braked → parked on its own mark forever, launch drive saw no train in seven minutes). **One process is one POSITION, not one platform**, which is what makes a three-position load platform three of these rather than a rewrite. `ETUSegmentZone::StationUnload`/`StationLoad` exist; an unload platform must NOT require locked restraints (nobody is aboard to close them). **Multi-position platforms work, and were mostly EMERGENT** — no sequencing rule exists or is needed, because a train cannot pass the train in front of it and the interlocking already says "advance when the space ahead frees". Two things did need building: `FTUTrackSegment::bStartsNewDevice` (three loading positions are the same kind at the same speed, so nothing the walk can see tells them apart), and **"loaded" belongs to the TRAIN, not the platform** — riders board once and the train advances full, or every train re-runs the whole sequence at every position and the platform never empties. Measured on three positions: 52 extra seconds of loading at the REAR costs the ride **5.5 s** and does not delay the two in front by a frame; at the FRONT it costs the full **52 s** and everybody pays. **AND THE SHAPE BEHIND THOSE TWO POINTS IS A BUDGET, NOT A CONSTANT (2026-08-08).** Two samples cannot tell a knee from a flat line; a sweep can, and every position turns out to have the same shape — **free time, then 1:1 beyond it**. The only thing that differs is how much: **43.6 s at the rear, 22.5 s in the middle, 0.5 s at the front**, found by bisection rather than a fine sweep because each sample is a 200-second three-train run. The front's is zero *by construction* — nothing can pass it, so every second it loads is a second the two behind it stand still — and the budget elsewhere is the time the queue in front takes to clear. **That is the number an operations team can use**: "move a slow party to the back" is advice, "the back absorbs about forty seconds and the middle about twenty before anybody waits" is a decision. Asserted as an ORDERING (each position back has more budget, and a delay further forward is never cheaper than the same delay further back) rather than as three magic numbers, so the check survives a layout change and still bites. **AND WHAT THAT MEANS FOR ORDINARY VARIATION IS THE SHARPER RESULT.** Held constant, a dwell is a figure; a real platform's is a spread. Over 40 randomised loads per position — **the curve is INVENTED and labelled as such**, since this project has no dwell data from a real park and what is being measured is the layout's *sensitivity* to variation rather than anybody's ridership — the rear costs **0 of 40**, the middle **10 of 40** (mean 2.6 s), and the front **34 of 40** (mean 12.4 s, worst 40.1 s). **The first version of that test asserted variation is "mostly absorbed" and the measurement said otherwise**: with all three positions varying, 54 of 64 runs cost something, because a run costs whatever its FRONT draw costs and the front has no budget. Stated properly: **the platform absorbs variation at the back and none at the front**, which reads operationally as something stronger than *put slow parties at the back* — **the front position is where predictability is worth most**, because every second of variance there is a second off the whole platform. Every draw is also a differently-timed sequence through the same interlocking, and none may violate; that is asserted too. **Not built:** turnouts, which the developer has parked as a far-future stretch goal. See `SIGNALLING.md` § "The station". **A backstage buffer is NOT a missing concept** — it is brake sections with drive tyres, one train each, which is a block brake and is already what the model expresses; ten trains stacked is ten trains in ten ordinary blocks. It is a layout decision for one specific ride, not something the general model owes anybody.
- **A BLOCK BRAKE IS TWO MACHINES, and the zone now says so.** A friction pad that can only remove energy and drive tyres that push and hold — separate hardware, specified separately, either able to fail without the other. The distinction that carries it is **a CEILING versus a SETPOINT**: the tyres are a setpoint driven toward from either side, the pad is a ceiling that leaves a train already under its limit untouched, so it **can never add energy however it is authored** — the guarantee lives in a `std::min(0.0, …)` rather than in a comment asking callers to be sensible. That is also, retroactively, *why* a trim cannot start a train. Asserted with a hostile value: a bare pad whose "limit" is 40 m/s holds a 10 m/s train at exactly 10.000000. **The pad tracks the same command rather than taking a second one** — a block brake is commanded to a SPEED and the two devices are two ways of reaching it, so the four-state sequence (bite, stop, release, convey) falls out of one number instead of a state machine, because *release* is simply the train no longer being above the limit. A **trim already IS a pad** and does not get a second one. Negative `BrakeLimit` means no pad, which is every preset, asserted bit-identical over 900 steps.
- **A run ends where the device changes, and the device is its KIND AND ITS SPEED.** Kind alone was the original rule, and it silently merged a brake at 6 m/s with one at 2 m/s into a single zone targeting 6 — a typed number discarded, which this project treats as a defect everywhere else. It was a logged warning with a wedge-a-different-kind-between-them workaround. Splitting on speed is the smaller and honest rule, and it is what lets several holding devices be authored in a row at all. Same kind *and* same speed still merges, which is correct — that is one device spanning several segments, and it is how every lift hill here is authored. **No preset changed**; every canonical figure is identical. **The device identity has since grown TWICE more, by the same argument each time**: its acceleration and deceleration (`ZoneAccel`/`ZoneDecel`, replacing one hardcoded `Grip = 6.0` for every device on every ride — a chain hauling at 0.61 g is nothing like a real chain), and its friction pad's rate. A brake biting at 8 m/s² and one at 2 are two different machines. All default to the old constants, so every measured figure is still unmoved.
- **The operator: manual dispatch and an emergency stop.** **The E-stop lives INSIDE `FTrackDrives`** and overrides the *output*, not the command — one layer up it would be a stop with a hole in it the width of every caller that forgot, and the one thing that must never have holes is the stop. **It does not stop trains, it stops the ride**, and that falls out rather than being arranged: open course coasts to the next brake and holds, a brake run stops at once. Measured on the circuit — tripped mid-lap with three trains, every one comes to rest at a device, no violation. Latched, first reason wins, cleared only by a person. **Three conditions trip it that were already detected and only logged**: a signalling violation, a drive fault, a block counted occupied twice. Manual dispatch is ANDed on the end of every other permissive and never instead of one; the button must be **released between trains** (anti-tie-down), and "not pressed" reads differently from "held from the last train". Keys: `[Space]` dispatch, `[Backspace]` stop, `[End]` reset. **The full set**: `[Tab]` mode, `[C]` camera, `[T]` which train, `[N]` which seat, `[F]`/`[Z]` frame, `[`/`]` step segment, `[B]` segment editor, `[M]` back to the menu, `[J]`/`[K]`/`[L]` undo, save, redo, `[I]`/`[R]` insert and remove a segment, **`[Y]` bank the selected turn (2026-08-26: the one macro — an arc becomes clothoid, arc, clothoid with the three roll pairs typed for you from the speed the profile measures there, one edit to undo; Sidewinder item 8, "banking typed three times per turn")**, `[Pause]`/`[.]`/`[,]`/`[/]` sim clock, `[Home]` acknowledge, right-drag to look, left-click to select. **THE INSPECTION KEYS ARE F1–F6 (2026-08-28)**: `[F1]` settings, `[F2]` hide every overlay, `[F3]` diagnostics, `[F4]` control panel, `[F5]` profile graph, `[F6]` its channel. Six keys that put a picture of the ride's state on the screen and **change nothing about the ride**, sitting together so somebody can find them by walking the row — and six letters (`O`, `U`, `V`, `P`, `H`, `G`) back in the alphabet. **What did NOT move is the point**: dispatch, the E-stop, the reset, acknowledge, save, undo, insert, remove. Those ACT on something, and a row of identical unlabelled grey keys is the wrong home for the control that stops a ride. They had all been letters because F1 flipped the world to wireframe while the UI changed, and **this file said for months that the EDITOR was answering to the same press. IT WAS NOT, and the misdiagnosis is the lesson (corrected 2026-08-29 from a forum thread the developer found).** The editor's view modes are **Alt+2, Alt+3 and Alt+4** (`FEditorViewportCommands`); it never wanted F1 at all. What fires is **`DebugExecBindings` in `Engine/Config/BaseInput.ini`** — console commands run by **our own `UPlayerInput`** out of config in **every non-Shipping build**. So *"run standalone"* never avoided it, and neither would focusing the game viewport or consuming the key in the binding stack: a debug exec binding is read from the raw key state and does not care what consumed what. **`Config/DefaultInput.ini` removes the five that clash** with `-DebugExecBindings=`, matched line for line against `BaseInput.ini` — F1's `bIgnoreShift` included, or the removal silently does nothing — and in the PROJECT rather than the engine's copy, which is somebody else's install, is not in this repository per constraint 4, and would be gone at their next engine update. **F9 (`shot showui`) and F11 (immersive) are left alone** because nothing here is bound to them; `RebindKey` warns if somebody takes one. **A Slate input preprocessor in front of the editor's command list was written, built and reverted the same evening** — it was guarding a door nothing was coming through, which is what a confident wrong diagnosis buys. **ONE KEY, ONE OVERLAY (same day).** Every overlay toggle used to clear `bHideOverlays` and nothing else, so with `[F2]` down, asking for the graph brought the console, the diagnostics list and the segment editor back with it — two keys turning each other on. Leaving the hide up instead is worse (the key looks dead and the state it silently changed is waiting to surprise somebody), which is why the clear was there. `LeaveHiddenForOne` is the third answer: come out of hidden with everything DOWN and let the key that asked raise its own. Nothing happens when overlays are already showing, so the ordinary press is untouched. **`[Backspace]` and `[.]` are SHARED with typed entry** and dispatch on `IsTypingInField()` — bound naively, editing a radius would E-STOP THE RIDE.
- **Stop categories (IEC 60204-1) are now EXPRESSIBLE, which they were not.** `FTrackDrives::EStopCategory`. Cat 0 is immediate removal of power; **Cat 1 is a controlled stop with power RETAINED to achieve it, then removed** — and the second half is the half people drop. **The operator's button is the only Cat 1 on the ride** (`PressEmergencyStopButton`) and every automatic protective trip is Cat 0: a person pressing the button has decided to stop and nothing is known to be broken, where a signalling violation or a faulted drive often means *the thing being asked to perform a controlled stop is the thing that failed*. **The default argument is Cat 0, deliberately** — Cat 1 is strictly the weaker stop, so a call site that forgets to say must get the harder one. Two things make it safe rather than a hole: **the brakes are fail-safe**, so a zone commanded to zero bites and a Cat 0 stops trains rather than merely ceasing to drive them; and Cat 1 carries a **delay timer** (`SetCat1DelaySeconds`, 5 s) because a real SS1 relay does not ask the drive whether it finished, it opens the contactor at the deadline regardless. **Ramps default to off, so on every shipped preset the two categories are identical and nothing measured moved** — the circuit's three-train E-stop measurement is unchanged. `Output()` now guards on *power removed* rather than on *stopped*, which is what lets Cat 1 keep driving while it winds down.
- **Drives, on the output side** — `BlockSignal/TrackDrives.h`. One per zone, and **the PLC's only authority over the ride is to write a command**; how fast the drive gets there is the drive's business. Three numbers that can disagree: `Commanded ≠ Output` is ramping, `Output ≠ Actual` is slipping, `Load = 1` is full torque (free — `Step` already clamps what a zone asks for to what it has). **A fault needs slip AND torque AND time AND not gaining**, and the last term is not optional: a launch is sustained slip at full torque *by definition*, so the first three faulted the real circuit's launch drive on its first dispatch. What separates a launch from a stall is whether the gap is CLOSING. Faults are **reported, never acted on**. **A failed brake is now expressible** — `SetDeliveredFraction` is what the hardware *delivers*, not what it was commanded, pushed to every train's copy of the zone alongside `Output` and multiplying both authorities at the clamp in `Step`. Health defaults to 1.0 and the digest of a full run is bit-identical, so nothing measured moved. What catches a dead device turned out to be **three different answers**: parked on it the ride never dispatches (the stop-mark rule paying for itself), loaded the interlocking and the counter both trip, and **sparse it is simply ABSORBED by the devices downstream and no rule is broken** — a failed brake on a well-laid-out ride is a *capacity and schedule* event, not a safety one, and the detector actually missing is "a train did not stop where it was told". Read the failed-brake section of `FAULTS.md`; an earlier version of this line claimed a runaway that measurement disproved. Ramping defaults to **off** so every pre-drive measurement is unmoved; a ramp only matters if it is slower than the zone's grip. This also ended a per-train wart: zones live on each `FTrain`, so every train used to carry its own idea of what every brake was doing — one drive's output now goes to every copy each frame.
- **A permissive that derives its braking distance** — `FRideSignals::SetHoldingBlocks` says which blocks can hold a train, and a dispatch then clears from the destination *to the next one of those*, because a train in a block with no device in it is committed. The fixed `Lookahead` count is now extra headway rather than the safety rule. **This is what makes the circuit carry four trains instead of colliding at four**, measured both ways.
- **NL2 CSV and telemetry** — validation fixtures, not authoring paths. `NL2Csv/`, `NL2Telemetry/`
- **A TRACK WITH NOWHERE TO PARK A TRAIN IS A REAL LAYOUT, AND IT CRASHED (fixed 2026-08-08).** N holding places run N−1 trains, one staying free so anything can move, and the floor of `Max(1, …)` existed so a layout with exactly one place still runs a train. **It applied at ZERO places too**, so `Place(HoldMidS[0])` indexed an empty array — and behind it `RunRideProfile(*Trains[0], …)` did the same. **A blank track with one straight on it is exactly that layout**, which is what the menu's Blank template and a first `[I]` produce: the very first thing a new author does took the editor down, twice over. The floor is conditional now (no holding device, no train) and a trainless rebuild takes a **default profile** rather than skipping the assignment — `bCompleted = false` is a case the graph and the diagnostics panel were already built around, so there is nothing new to teach them. Reported **plainly and without alarm**, because it is the normal state of a track being built: *"no train yet — nowhere to park one. Give a segment a station or a block brake and a train appears on it."*
- **A NEW DOCUMENT IS NOT A MODIFIED ONE.** `StartFromTemplate` called `DidCreateNew("")` *before* the rebuild, and the rebuild then `Observe`d the real text — so saved was empty, current was a document, and **every template landed in Build already reporting unsaved changes**. Nobody had touched it; the frame showed the asterisk and going back to the menu asked whether to discard work that did not exist, which is how people learn to click through the one prompt that matters. Baseline taken after the rebuild now, the same order an open uses and for the same reason: dirty is a COMPARISON.
- **Starter layouts** — five worked examples of the vocabulary behind a `Preset` dropdown: flat rig, out-and-back, reference, two-train circuit, small batch. Each was measured before shipping. `ATUCoasterRide::PresetLayout`. **`SmallBatch` is the two-train oval re-zoned**, not new geometry — 6 m vehicles, an unload platform and three loading positions, 8 holding places so it carries 7. It is DERIVED from `TwoTrainCircuitLayout()` rather than copied, because the closure is a property of the LEG LENGTHS (leg A is 176 m of flat either way) and a copy could drift from the shape it depends on with nothing to notice. **A preset now brings its train with it** (`ApplyPresetTrainSetup`, explicit load only) — the train is part of the worked example. **AND `Showcase` IS THE MILESTONE RIDE (2026-08-09)**, derived from `SmallBatchLayout` for the same reason SmallBatch derives from the circuit: closure is bought by the LEG LENGTHS and a copy could drift from the shape it depends on. It exists because two device features were built, tested and shipped with **no ride using them** — `ZoneBrakeDecel` was 0 on every preset, so the "a block brake is TWO machines" model had no instance, and the closed circuit had never had a trim. The trim is **split out of the fill straight** rather than added to it, so the leg keeps its length. Devices are specified individually instead of all running the default 6 m/s², and **the launch gets its anti-rollback back** — SmallBatch replaces exactly the segments the circuit sets it on, so its launch and climb had none. Asserted in `-TUSmokeTest` rather than eyeballed, because a showcase that stalled its train or missed the seam demonstrates the opposite of what it is for and both are silent from the cockpit. Measured **2026-08-21**: **34 segments, 1288.0 m, closes, top 136.8 km/h at 115 m** (154 m before the punchier launch), 107 s, −0.94..+3.51 g vertical, 0.35 lateral, 2 pads, 1 trim, 6 trains. The duration and the lateral moved with the helix work and this line went on saying 109 s and 0.30 — re-read them off `-TUSmokeTest`, which prints all of them, rather than quoting this paragraph. The vertical figure is the **6 m train**, not the showcase — SmallBatch already reads +3.46 on the same vehicles, and a shorter train straddles less of the crest.
- **A device shorter than its train is REPORTED at build time.** The stop mark is `max(start + trainLength, end − clearance)`, so on a too-short device the mark lands *past the far end*, nothing trips it, and a train sent to park there crawls out of its block into the next one. Measured: a 10 m position with a 15 m train gave seven violations in four seconds. Reported, never repaired — clamping the mark back inside builds a device that stops a train with its nose over the boundary, which is worse because it looks like it worked.
- **THE DEVICES ARE AUDITED AGAINST THE RIDE THEY PRODUCE** — `BlockSignal/DeviceAudit.h`, wired into the `[V]` panel on every rebuild. **This could not be a `TrackValidate` rule and that is structural**: the validator reads AUTHORED values and can judge them with the numbers alone, where whether a brake is long enough depends on the train's length and whether it can stop what arrives depends on the speed there — properties of the RIDE, which only exist once the profile does. **The point is the SENTENCE, not the detection**: not *"block brake at 412 m is too short"* but *"a train arrives at 30.4 m/s and needs 154 m to stop at 3.0 m/s², but only 129 m is usable. It leaves the block at 12.3 m/s. Lengthen it by 25 m, brake harder, or slow the train upstream."* The first is a rule somebody must look up and then decide whether they care about; the second IS the failure, and happens to say what to change and by how much without choosing which of three defensible answers is right. Four checks, and **the SILENCE assertions are what keep them honest** — a healthy brake reports nothing, and a trim that is not a block boundary reports nothing, because trimming is what a trim is for and a checker that complained about every trim on every ride would be switched off within a day. It reads **each device's own deceleration**, not a global figure, and on a section with a pad judges with the harder of the two — a single number for every brake is a second source of truth that disagrees with the physics in whichever direction the guess was wrong. **AND IT CHECKS THAT LIFTS LIFT (2026-08-26)**: the audit checked brakes stop and never that a chain could haul its grade, so a chain with accel under g·sin(grade) stalled silently on the Sidewinder. `CannotHaulGrade` reads the steepest grade under each tractive device from one coarse walk of the track and reports *"climbs 25.0 degrees and can push at 3.0 m/s², but holding a train on that grade takes 4.1 before rolling resistance — raise the accel above 4.1 or ease the grade below 17.8°"*. Same day, **a segment that cannot be built says so with the consequence**: `FTrack::AddSegment` refuses a non-positive length and `BuildTrack` drops it silently, so a turn whose easements ate more than the turn lost 9.7 m of arc four times over and every later device sat 39 m early with the circuit still closed by symmetry; the validator's `BadLength` row now reads *NOT BUILT: everything after it sits 9.7 m earlier than authored*, the design program fails on any validator error and on a dropped segment, and the panel shows the row even when nothing at all built (the empty-track early return used to skip `BuildDiagnostics`).
- **A BOUNDARY THAT KNOWS WHICH WAY** — `FDirectionalCounter` in `TrackSensors.h`. `FBlockCounter` is proven equal to perfect knowledge over three laps **forwards**, and that was a belief about the other direction until it was measured: reverse a 20 m train over a boundary and it leaves `block 0 = −1, block 1 = 2` where the truth is 1 and 0. **It fails SAFE** — −1 is what the counter already calls a lie and 2 is the collision condition, both E-stop conditions — so a reversing train stops the ride loudly rather than running on an interlocking that quietly believes the wrong thing. The fix is **a second head, not a cleverer rule**: two heads a surveyed gap apart and the ORDER of their edges gives direction, exactly as a real axle counter does and the same idiom the speed trap already uses. The rule is symmetric, so **every event has an exact opposite** — asserted by oscillating a train five times and requiring the counts to return exactly. A dead head counts NOTHING rather than guessing; both heads in one scan is DROPPED, because there is no order left to read. **Nothing consumes it yet.**
- **WHICH WAY THE SEAT FACES** — `RunRideProfile(..., FacingSign)`. **Travelling backwards was already correct** and the design doc claimed otherwise until it was checked: `LastTangentialAccel` is a signed dv/dt measured along +S, so a train braking while running backwards reports `+Gx` and a forward-facing rider really is pressed into their seat. **A rider FACING backwards is the wrong case** — backward-facing seats, face-off trains, a train a turntable spun — and lateral flips with fore-aft, because reversing the tangent while keeping up requires reversing lateral or the frame is no longer right-handed. Vertical is untouched. **A parameter of the profile run, not a field on the train**, because facing belongs to the SEAT: a face-off train has both at once and the honest way to measure one is to run it twice.
- **The ride profile, JUDGED** — `Prototypes/TrainPhysics/GEnvelope.h` puts the profile against the duration-dependent ASTM F2291 / EN 13814 acceleration envelopes. **The limit tables are UNVERIFIED research, not a copy of either standard** (both paywalled), and the header, `PROTOTYPES.md` and the suite banner all say so — bands are *data*, so a verified table is a one-function edit. Four things are load-bearing and each was found by measurement: a limit is a curve against **duration**, so bands are stored inverted (*no run above 4 g may last longer than 1 s*) and the backwards reading silently passes 5.9 g for 3 s; the profile is **resampled to uniform time** first, because arc-length sampling measures the train's speed rather than the rider's exposure; the **5 Hz Butterworth runs forward AND backward**, because a single pass reports the violation half a second downstream on innocent geometry; and an **impact is reported only when the sub-0.20 s exemption is what saved it**, or every ride reports wobbles over the 1 g band it rests at. **Report, never repair.** **AND THE COCKPIT NOW READS THE SAME SEAT (2026-08-29)** — `JudgeRideProfile` had judged the outer seat since 2026-08-22 while the live telemetry went on reporting the heartline, which is two answers to one question. `OffsetFeltG` is the one formula both go through now, and `FTrain::RollMotionAt` gets ω and α **from arc length rather than from the clock**: roll is a property of the track, so ω = v·dRoll/ds and α = v²·d²Roll/ds², differenced over the train's own sample frames — where a per-frame time difference would be a second derivative of a number that changes with the render rate, which is the noisiest thing a readout could be built on. Unwrapped pairwise, or a world-bank segment's atan2 step puts several thousand g on the outer seat. **The centre seat is y = 0 and reads exactly what it always did**, so no measured figure moves; the omitted term is α += (dRoll/ds)·a, a few per cent here and worth having the day a launch fires through a barrel roll. Its real-ride test **asserts the train completed** — that is not decoration, the suite reported "within envelope, zero findings" three times over a train that stalled at 46 m, and a conformance verdict on a ride that did not happen is the one failure this tool must not have.
- **The canonical figures, reproducible** — `Prototypes/TrackSpline/reference_figures.cpp` prints everything `Docs/REFERENCE_LAYOUT.md` publishes. Run it before quoting a number from that page.

- **The track, as geometry you can see** — `Prototypes/TrackMesh/`, and Phase 4's first piece. `WalkTrack` is the ONLY thing that touches an `FTrack` and it walks with `AdvanceFrom`; `BuildTrackMesh` takes the frames and has no `FTrack` at all, which is what makes the O(n²) trap **unreachable rather than discouraged** — the vertical slice's debug draw had exactly that bug, ~118 M integrator steps at BeginPlay. Rails, spine and ties are three buffers because they are three materials, and a tie is **two struts rail-down-to-spine** rather than one bar across, because a real cross-tie is a truss and a bar would pass through the train. Every assertion covers geometry that LOOKS plausible: **inside out** (found one immediately — `Cross(along, around)` is *minus* the radial normal where `Tangent × Lateral = Up`), the **UV seam** (a ring closes, its texture coordinate does not), and a **curve tighter than half the gauge** folding the inner rail through its own axis, reported never repaired. **THE PORT RULE, AND IT WAS BACKWARDS UNTIL 2026-08-09:** `M(x,y,z) = (x,−y,z)` is a REFLECTION with determinant −1, so it **reverses triangle orientation** — and the port must therefore leave the index order ALONE, because UE's opposite handedness reverses it a second time. Swapping was the original reading and it inverted every surface on the ride. **Open ends are CAPPED, and a circuit's are not** — on a closed layout the first ring and the last are the same ring, so a cap there is a disc buried in the seam, coplanar with what is either side of it. `bCapEnds` follows the *measured* `bTrackIsCircuit`. **Ties are capped either way**, because a strut has two free ends by definition and that is where it showed: every tie was a length of open pipe with both ends in shot at once. Asserted by **signed volume and watertightness**, which is stronger than the winding check it joins — a closed mesh encloses positive volume only when every triangle faces outward, so one reversed cap drops it by two thirds, and reversing one is asserted to be visible. A 1288 m circuit at 0.5 m / 12 sides is 234,533 vertices and 309,192 triangles; **caps cost the ties double** (a 12-side cap fan is as many triangles as a two-ring strut's wall), which is the whole +25%.

- **Supports, and where one must NOT go** — `Prototypes/TrackMesh/TrackSupports.h`. **DRAWN AS OF 2026-08-08**: `BuildSupportMesh` sweeps each `FSupportLeg` with the same `SweepStrut` the ties use, into a **fourth mesh section**, because a column is a fourth MATERIAL — steel is not painted rail, and a section is the unit of material in the engine. **EVERY LEG LANDS ON SOMETHING, and the track too low for a leg lands on one directly.** A column does not stop in the dirt — and the `MinHeightM` branch had said *"the track is essentially on the ground and wants a footer plate, not a tower"* since it was written, then placed **nothing**. Invisible while nothing was drawn; obvious the moment legs were, because the lowest run of a layout floated with no column and no pad, which is the one part of a real coaster nobody has ever seen unsupported. Footings are a **separate list, not a field on the leg**, because the two do not correspond one to one — at-grade track has a footing and NO leg — and keeping them apart leaves the tested claim that at-grade is *silent* exactly as true, since that claim is about REPORTING. **On the vertical slice: 62 legs on 134 footings**, so seventy-two pads under track that had nothing before. **The footing dimensions are ordinary practice and say so** — Self (2024) puts foundations explicitly outside its scope, so nothing here rests on that source. Held to the same bar as the track and for the same reason: watertight, outward-wound by **signed volume against the N-gon prisms the legs should enclose** (18.138907 m³ against 18.138907 m³ on the hill), and reversing one triangle drops it. **Eight sides where the rails get twelve** — a column is background, seen at distance and in quantity, where a rail is under the camera in a ride view. **Its own walk at 1 m**, coarser than the mesher's 0.5, because a span is 9 m and eighteen candidates per span is eighteen times the work for one answer. **A plan with no legs builds nothing** rather than something degenerate, which is what keeps a refusal a refusal all the way to the screen. The findings stay **opt-in**, now for a better reason than before: a hole is VISIBLE, so the list repeating it is a second answer to a question the picture already gave — what it adds is the metres and the WHY. Placement is a constraint problem with no single right answer, so this places what the obvious rules allow and **REFUSES the rest with a reason** — the conclusion the evacuation walkways reached. **Spacing is a SPAN, not a sample count** (asserted by changing the walk spacing and getting the same bent count). Track under grade gets nothing rather than a column of negative length; **at grade is silent, because that is a station**. Through an **inversion the spine is ABOVE the track**, so a column from underneath would pass through the rails — detected from the frame's own up vector. And **a column must not pass through the track**, the classic failure, asserted both ways: a return leg over its own outbound refuses all 48 m above it, a plain hill refuses nothing. **Findings merge into RUNS by KIND** — merging on the message looked right and was not, because every fouling finding names the metre it fouls at, so nothing ever matched. The **longest unsupported run** is reported, because a placer that reported only what it placed says nothing about the hole its refusals left. Ground is a **function**, so Phase 5's terrain is a swap.
- **Z-ORDER IS DRAW ORDER, and it was backwards** (2026-08-08). Everything rides one debug-draw callback deliberately — two registrations can be ordered independently, and one panel painting over another by registration order only appears on somebody else's machine — so draw ORDER is the only z-order there is. The drag answer and the unsaved-changes confirm were drawn FIRST, under the segment editor and the diagnostics list; the confirm even carried a comment claiming it was last. They could not simply move to the bottom of the draw, which returns early in three places, so `DrawPanels` is split out and the menu, the drag answer and the confirm are drawn after it.
- **MOUSE SENSITIVITY AND INVERSION ARE PER CAMERA AND PER AXIS** (2026-08-08). It was one bool flipping both cameras' Y, on the stated argument that *"somebody who wants Y inverted wants it inverted"* — **and that did not survive its first user**, who found orbit backwards and free-fly right. That is exactly the case one knob cannot express, and the reason is the disagreement the same paragraph describes: orbit drags the SUBJECT and free-fly turns your HEAD, so they are two different gestures and a preference about one is not evidence about the other. The **defaults still encode each camera's own convention** and the toggles flip from there, never from the raw axis — so ticking *invert* gives the opposite of what somebody was just feeling, which is the only useful reading of the word. Orbit is the camera in **both Build and Operate**, so one pair covers both. `input.invertLookY` is retired rather than reused: keys are permanent, and anybody who set it keeps the line untouched by the unknown-key rule.
- **THE DRAG QUESTION IS ANSWERED WHERE IT IS ASKED** (2026-08-08). `FirstRun.h`'s `WhyCannotIDragTheTrack()` was written, reviewed and **shown to nobody** since the day it landed — a help menu files it where only somebody who has already accepted the answer would look. **The trigger is the gesture**: a left-DRAG that started on no panel, in BUILD, past twelve pixels. Not a click — clicking empty space is how you deselect, and a wall of text on every one would be the most annoying thing in the application. It **times out rather than being dismissed**, because it is an answer and not a dialog: nothing is blocked and nothing needs acknowledging. It ends with what to do INSTEAD, since an explanation with no next step is a refusal with paragraphs. `ViewportHint()` carries the one-line version in Build **until there is a selection** — somebody who has clicked a segment has found the panel, and a permanent line about a constraint that is not currently biting is chrome people stop seeing.
- **The shell, engine-free wherever it has an answer that can be wrong** — `Prototypes/Shell/`. The **camera**: framing checks BOTH axes (the distance that fits a box vertically is not the one that fits it horizontally, so a long low layout hangs off the sides of an ultrawide) and uses the bounding SPHERE so orbiting cannot lose the subject; the **near plane follows the camera**, because a 90 m lift and a 2 cm bolt cannot share a fixed one; pitch CLAMPS rather than wraps; zoom is MULTIPLICATIVE; and smoothing is **frame-rate independent**, so the camera does not feel different on a better machine. The **diagnostics panel**: report-never-repair made structural — a row has a place to GO and no action to TAKE, so there is nowhere to wire a fix button — **height gets its own row and says LOW or HIGH** (the vertical slice shipped 8.5 m low because plan view looked closed), a ride that does not complete **hides every derived number** (the envelope suite's own old failure), and **saving is never blocked**. **Two rules moved out of comments onto it (2026-08-26)**: a helix entered off its climb angle gets a row with both angles and the fix (*"entered pitched 0.0 deg but climbs at 10.0, so its axis tilts 10.0 deg"* — `MakeHelix`'s header had said so since Phase 0 and the Sidewinder's exit clothoid turned 13° of pitch before anyone read it), and the station's height above the ground is stated as the rule it is — the supports take the track's lowest point as the ground — rather than learned from the legs appearing. **And energy is no longer invisible until the train stops** (Sidewinder item 6): `TightestCrest` in `RideProfile.h` reads the speed over every *unpowered* crest as metres of climb still in hand, v²/2g, and the Ride row names the one with the least — *"tightest crest: at 341 m the train crosses 26.0 m up at 5.0 m/s, which is 1.3 m of climb in hand"* — a Warning under a metre, because that is where a slower load or a wet rail stalls the ride. The lift's own crest is the chain's business and is skipped; a stalled ride has its own row and no crest to read. The **segment editor**: **IT CAN AUTHOR AS OF 2026-08-21, AND COULD NOT BEFORE** — `EEditField` had no `Kind` entry and roll wrote only `RollEndDegrees`, so a blank template plus `[I]` gave STRAIGHTS FOR EVER and every curve on every shipped track came from a preset or from the developer-only Details panel. **PROJECT_PLAN gives Phase 1 the gate *"build an arbitrary coaster from scratch in-editor"*, and the SHIPPING path had never met it with the whole Phase 1 list ticked.** Kind cycles on click, which is the shape `Device` already had and is not constraint 1's direct manipulation — nothing is dragged and nothing is placed, it is the same discrete pick the Details panel offers without leaving the ride you are looking at. **RAW IS NOT A CYCLE DESTINATION**: it is a sampled curvature profile the importer produces, it has no authored parameters to show, and cycling into it would hand somebody a segment they can neither edit nor leave — a raw segment cycles OUT to Straight. **Roll is a PAIR and only the end was writable**, so a hand-authored bank started at 0 on every segment and STEPPED at each joint, which `TrackValidate` then correctly complained about — the tool being honest about a hole it could not fill. **`IsChoiceField` is now one answer** because it was two: the panel kept a hand-written list and the help suite kept another, and adding `Kind` made them disagree immediately — the suite asserting that a dropdown ought to carry a typical range. Asserted end-to-end in `-TUSmokeTest` on the GEOMETRY rather than the field, because a kind nothing downstream read would pass every other check: **from a blank track, `[I]` then the kind row gives 40.0 m turning 91.7°, banked 5 to 25**. **AND THE PANEL SAYS WHAT A SELECTION ACTUALLY DOES (2026-08-26)** — `SelectionSummary()`: one walk over the selected rows off the built track, *"segment 3: 40.0 m, turns 91.7 deg left, climbs 0.0 deg"*, heading unwrapped so a full turn reads 360. It exists because a helix row says `0.79 turns` and its easements turn too: on the Sidewinder the helix reads 284.0° alone and 358.4° with its two pitch pairs and two easements selected, and nothing on screen had ever added them up (Sidewinder UX list, item 2). **Found on the way: five places summed the STORED `Length` field to find where a segment was** — the selection framing, the preset walkways, the row list — and a Helix (now a Pitch) row's stored Length is whatever default it was born with, not what it builds to. `SegmentLengthOf` (through the same `BuildSegment` the track uses) is the one answer now. Also `EditConditionHides` reimplemented, and **hidden is not deleted** — the naive version clears a value when its field stops being relevant, so flipping Arc→Straight→Arc loses the radius; multi-select shows the INTERSECTION and flags a differing value rather than showing the first (which people then edit on top of) — **WIRED 2026-08-08, having computed that intersection and been asked by nobody since it landed**. Shift-click extends, and **shift is already the camera boost**, so it dispatches on CONTEXT exactly as `[Backspace]` and `[.]` do on a focused field: a shift landing on a segment row is a selection, one that does not is still a boost, and nobody is flying the camera and clicking a row in one gesture. **Why it is worth having on a coaster specifically**: banking a turn is one number typed into eight segments, and typing it eight times is not tedium — it is eight chances to type a different one, and the result is a bank that STEPS rather than ramps, which the roll-rate validator then quite correctly complains about. A field only some of the selection uses is **not offered**, because writing it would give an arc's radius to a straight — a value that does nothing until somebody changes its kind and then does something nobody asked for. A differing value reads `-- (differs)`; typing over it still writes all of them, which is the intent, but you are told what you are replacing. The undo label carries the COUNT, since *"roll on 8 segments"* and *"roll on segment 12"* are different things to be about to undo, and an undo restores the whole edit rather than needing eight presses. The model is built over **the selection only** — a CSV import is four thousand segments and this runs to draw a panel; and the **undo merge key** `TrackHistory` says is the UI's to supply, broken for free by any structural edit. The **graph**: nice-number ticks AND a range grown to a multiple of the step, zero kept on screen for signed channels, a dead-flat channel given an axis rather than a NaN, and a scrubber that round-trips exactly — **DRAGGABLE SINCE 2026-08-08, having been tested and unwired since it landed**. What it is for is not reading a number off the trace (the extremes already carry their locations, which is the same rule twice): it is that **a graph and a track are two pictures of one ride, so dragging one moves the other** — you find the 4.2 g and then you are *looking at* the piece of track that does it, which a screenshot of a trace can never do. The handle is drawn by converting the arc length BACK to a fraction, which is exactly why the round trip is asserted: a mismatch makes it drift away from the cursor as you drag. It **does not clamp to the rectangle** (a drag that stops responding because the pointer left a box reads as broken), it **ends wherever the pointer is** (a scrub left running is a camera that keeps following it), it moves **only the orbit focus** (the free camera is somewhere somebody flew deliberately), and it **refuses on a stalled ride**, since a scrubber over a blank panel would move the camera to a metre that means nothing. `GraphRect` is one answer shared by the draw and the hit test, and takes a viewport HEIGHT rather than a canvas because there is no canvas outside a draw call. The **browser**: a file that will not load still gets a row **carrying the loader's reason**, and a missing file **stays in the list marked** rather than being pruned, because the commonest cause is an unplugged drive. And the **session and settings**. **Dirty is a COMPARISON, not a flag**: undo back to the saved text and the document is clean, which is only affordable because the save format is already the identity. **Autosave writes a SIDECAR and never the document** — overwriting on a timer is data loss with extra steps — and does not run on a clean document. **Crash recovery is OFFERED at BOOT, never applied**, because opening anything would overwrite the evidence, and an accepted recovery is **DIRTY** or somebody loses the same work twice. **BOOT IS REAL AS OF 2026-08-08, and it is what let autosave be wired at all** — the sidecar existed for recovery to offer and nothing could offer it, so writing files nobody could recover from was half a feature and stayed unbuilt on purpose. Now: boot discovers `Saved/Recovered.track`, `MayEnter` refuses to leave Boot while it is pending, and the offer is drawn above the menu because opening a document is what would destroy it. **The sidecar is deleted only on a DECLINE**, and by a save, which supersedes it — leaving it behind means the next launch offers work that is already on disc, which teaches people to click past the one dialog that matters. **A sidecar that will not parse is still evidence** and is left alone. **ONE FIXED NAME**, because recovery has to find it with nothing to go on: the application did not shut down, so nobody wrote down what was open. **Where the application STARTS is a property of the LAUNCH, and it was a compile-time test until a standalone run walked straight past the menu into Build**: `WITH_EDITOR` does not mean *an editor is running*, it means editor support was COMPILED IN, and Play > Standalone Game runs the same editor binary in another process. The test is the WORLD — **PIE alone skips boot**, because there you pressed play on a level you are editing, so the LEVEL is the document and a menu has nothing to open. Standalone boots exactly as a packaged build does, which is the whole point of the mode: a path only exercised in a build nobody has made is a path nobody has tried. `-TUBootMenu` still forces it, and now covers only PIE. **And the menu has NEW, OPEN, RECENT, SETTINGS AND QUIT** — all five. **OPEN IS TWO DIFFERENT ACTIONS AND THE ROW SAYS WHICH**: the editor gets a real `IDesktopPlatform` dialog (guarded, and the *include* is guarded too — an unguarded one is a link error in the one configuration nobody compiles until release day), and a packaged build **reveals the tracks folder** instead, because `IDesktopPlatform` does not ship and an in-application file browser is real work for a job the OS already does well. Drop a `.track` in and it is a row. A dead row logging a warning nobody reads was the alternative, and *"the button does nothing"* is the commonest complaint about any shell. Both land in **RIDE**, per the rule that the action picks the mode. Quit shares the unsaved-work prompt with going back to the menu because both discard the document and two prompts that read alike and behave differently is worse than one.
- **THE MODE DECIDES THE VIEW, INCLUDING AT STARTUP (2026-08-08).** That rule was applied on every mode CHANGE and never once at start, so a level opening straight into Build kept whatever camera was serialised — `Rider`, with no train moving, which is a view from **inside the track at z = 0** looking at the inside of a rail. `ApplyAppMode(Session.Mode())` at BeginPlay puts Build on orbit, and the orbit branch frames the whole track on its first tick because nothing has framed it yet. Going back to the menu asks; Build↔Operate↔Ride never does, because nothing is discarded. **`EditsAllowed()` is true in BUILD and nowhere else — constraint 1 one level up.** In settings, **a default is not a value** (only explicit choices are written, so improving a default reaches everybody who never touched it) and **unknown keys survive**, so running an older build once does not silently reset what a newer one added.

- **AN UNCONFIRMED VIDEO MODE IS NOT A CHOICE (2026-08-29).** Windowed Fullscreen was being picked in the settings and the application came up WINDOWED at exactly the desktop resolution — the worst of both, because it looks nearly full screen, the taskbar is still there, and the title bar is off the top so the window cannot even be dragged. **The mode was not being lost, it was being OVERRIDDEN**: a standalone launch carries `-windowed` on its command line, nothing here ever applied the saved settings afterwards, and the next write then saved the override over the person's choice. The ini said it plainly — `FullscreenMode=2` against `LastConfirmedFullscreenMode=1`. **That disagreement is the engine's own signal and it was being ignored**: `UGameUserSettings` carries a confirm/revert pair for exactly this, so a mode a PERSON chose can be told apart from one that arrived some other way, and **neither half had ever been called here**, which is what let an override become permanent. Now the settings screen **confirms** on write (they meant it) and `ApplySavedVideoMode` at BeginPlay **reverts an unconfirmed mode to the last confirmed one and applies it with command-line overrides off** — `ApplySettings(false)`, where the `false` is the whole fix. **Game worlds only** (in PIE the window is the editor's), **and not when `FApp::CanEverRender()` is false**: `-TUSmokeTest` runs `-game -nullrhi`, which is a game world by every other test, and `ApplySettings` ends in `SaveSettings` — a headless run is not allowed to write a headless window mode over the settings of the machine it runs on. Measured: the fix is what corrected a real ini, 2 → 1.

**THE SETTINGS ARE WIRED, and three things fell out of wiring them** (2026-08-08). `FSettings` lives on the actor beside `Session` and the `Browser`, loads `Saved/TrackUnlimited.cfg` at BeginPlay, and the generated screen reads and writes through it — so the half of the schema this project owns is no longer a screen that logged your preferences and forgot them. **THE LEVEL'S AUTHORED VALUE IS THE DEFAULT, not the table's**: `SimHz` and `bInvertLookY` are `UPROPERTY`s somebody may have set in the Details panel, so they are re-`Declare`d over the schema's figure at load — otherwise BeginPlay silently applies 240 over a deliberate 480, which is "a default is not a value" failing from the level instead of from an old config. One rule, no exception list, and the screen then displays what the ride is actually doing. **RESET IS A DELETION AND THE SCREEN WAS GOING ROUND IT** — `OnResetPage` wrote each default's *text* back through the edit path, which records today's default as a choice and pins the one person who pressed reset to it for ever; `ResetValue` erases instead, and `test_settingsschema` now asserts a reset page leaves nothing written and picks up a later default. **A SLIDER WRITES THE FILE ON RELEASE, NOT PER FRAME** (`OnMouseCaptureEnd`) — the value still applies as you drag. Bindings are **not** in that store: `FInputMap` holds them, because the suite already asserted a key belongs to one home and not two. **`sim.pauseUnfocused` is now real** — no time reaches the accumulator while another window has focus, which is the same mechanism as every other clock control, and it is kept separate from `bSimPaused` so coming back from a coffee does not claim an operator paused the ride.
- **AND THE CONTROLS PAGE IS CHECKED AGAINST WHAT IS ACTUALLY BOUND** — `CheckBindingsAgainstInput`, run at the end of `SetupPlayerInputComponent`. The schema's key list is a second list beside the `BindKey` calls, and it had already drifted: it said **F2** for the overlay toggle for one commit after F2 was given back to the editor's wireframe view, and nothing could see it. Now every `Kind::Key` row is looked up in the input component's own `KeyBindings` and a row promising a key nothing answers to is **reported, never repaired** — the table cannot know which half moved. Measured both ways: silent on the real set, and reintroducing the stale F2 produces `the settings page offers [F2] for "Hide all overlays", and nothing is bound to it`. Four missing rows were added while it was being written ([H], [O], [,], [/]).

The editor surface is Unreal's Details panel over `TArray<FTUTrackSegment>`, with a live viewport preview and ride-profile traces. That is not a placeholder for a Slate UI — see the numeric-entry card for why.

**Phase 2's bar is "feels right to an NL2 veteran."** The thing this file used to send you at — the train being a point with no length — is **done**: `FTrainConfig::TrainLength`, nine sample points, gravity reading the train's *mean* height. Two claims that went with it have since been measured and were wrong, so do not repeat them. The back car is only thrown harder on an **asymmetric** hill, and on a symmetric one there is no difference at all. And train length does **not** explain the `DragK` fit — the two coefficients were never separable, correlation 0.975, so that split is a free parameter rather than a result.

Two trains on one circuit is **done** (2026-08-03) and the actor runs them, so do not go looking for it. `Prototypes/TrainPhysics/test_twotrains.cpp` is the proof, and the last test in it is a line-for-line stand-in for `ATUCoasterRide::Tick` — that exists because the actor cannot be compiled without Unreal, so its dispatch policy has nowhere else to be checked. **If you change the actor's tick order, change that test with it or the policy is unverified.**

**A real ride's brake count is not its block count.** Three different things sit on the track and all look like a brake: **block brakes** (interlocking — the only ones that bound a block and the only ones the capacity formula counts), **evacuation zones** (getting riders *off*, which needs a walkway and an egress route, not just a way to stop), and **safety catches** (anti-rollback; passive, fails closed). A large ride can have ~25 evacuation zones and a handful of catches while running far fewer trains than either count.

- **Anti-rollback is built** — `FTUTrackSegment::bAntiRollback`, derived into spans exactly as zones are. Deliberately **not** a zone (a zone is a *control* device with a speed and something commanding it; a catch has neither, and overlaps zones freely because a lift hill is both at once) and **not** a block boundary (it cannot release a train, so it is no more a place to park one than a trim brake is). A caught train is *arrested*, not merely stopped from moving — otherwise it keeps its backward velocity while pinned, which no hardware can do.
- **CATWALKS ARE DRAWN (2026-08-08)** — `Prototypes/TrackMesh/TrackCatwalk.h`. `ETUWalkway` has been authored per span since the evacuation model was built and consumed by `Evacuation.h` to decide whether a stopped train can be REACHED, and none of it was ever drawn: a safety system reasoning about a walkway nobody could see, with a gap in a route as a log line rather than a hole. **A CATWALK IMPLIES ITS HANDRAIL** — one toggle, because a walkway without a rail is not a cheaper walkway, it is a fall hazard 40 m up; NL2 independently ships one checkbox reading *"Left Railing and Catwalk"*. **Two buffers, because two materials** (NL2 gives deck and handrail separate colour pickers). **The deck is a CLOSED SLAB, not a one-sided strip** — a lift hill is looked at from underneath more than from above, and closing it buys the signed-volume check. **The rails are ONE swept tube each, not a strut per frame**: strut-per-frame was the first version and put 5088 triangles on 25 m of handrail, almost all of them caps buried inside the next segment. **Track too steeply banked to walk on is REPORTED and still drawn** — the author asked for it, and dropping the geometry hides the thing being complained about. **AND A PRESET NOW CATWALKS ITS DEVICES**: every shipped preset authored *none*, so no ride here had an evacuation route at all. Derived from the powered runs rather than typed as arc lengths — that is where a real ride puts them, and it survives an upstream segment changing length, which is the cost `FTUWalkway`'s own header warns about. Measured: the launched circuit catwalks **3 powered runs, 408 m of route**.
- **AIRGATES MOVE (2026-08-26).** The station platform (`TrackMesh/TrackStation.h`) drew one fixed panel per car while the CONTROL side — `FCommandedBank`, the console's GATES selector, a sensed group per section, the dispatch permissive's contact — had existed since 2026-08-04. The platform is 4 m wide with a **1.6 m operator's strip** between the stripe and a **fixed fence** (posts, two rails, vertical bars), with a **narrow gate per seat row** in it — 0.9 m, laid back from the zone's stop mark so each faces the seat it serves, per the NL2 reference shots — hinged on its upstream post and swinging **TOWARD THE TRAIN, into the strip**, by `(1 − position) × 90°`. The developer's rule, from the platform: an automated gate does not swing into a queue of people, and the strip between gate and train is controlled space *because the harnesses stay locked until the gates have opened* — **which the crew now enforces** (`FAutoStationCrew::Serve`: bars hold their last command until `Gates.IsFullyOpen()`; they used to be thrown in the same scan, asserted). **Three wrong versions shipped within the hour and were reported from screenshots**: a whole 2.7 m fence section per car swung off the far edge of a 2.5 m platform; a right-sized gate swung into the queue; and **a gate with no car at it opened onto the track** — the small-batch preset's 6 m train on 10 m positions has three gates a position and two cars. **A gate opens only onto a car** (`StationGateServed`: the row's centre within a train's span, the short way round on a circuit — standing in for the car-in-position sensor a real section has), whatever the bank is commanded; asserted in the suite and in `-TUSmokeTest` on that preset. The developer's framing, worth keeping: these are safe-operations lessons, and it is easy to make assumptions. Rails end on the posts' *faces*: two rails meeting on a post with identical end rings weld into one open tube and the watertight check said so. `StationGateCentres` is one layout for the mesher and the actor's gate-to-section mapping. `FCommandedBank::GroupPosition` is `FCommandedBank::GroupPosition` is the bank's physical position per group: one rate over `TravelSeconds` from wherever the last command found it, and **a jammed section holds the position the jam caught it at** — so it stays visibly open while the others close, the first time that fault has been anything but a line on the maintenance panel. **The mesh subscribes and never commands** (Show is read-only). The actor keeps the station's walk, spans and settings from the rebuild and re-sweeps only the steel buffer, only when a position changed, updating vertices in place as the train does — asserted in `-TUSmokeTest`: 8 gates, jammed section stays open at 0.00 while the rest reach 1.00, midway 0.50, 920 vertices with no section recreated. **AND THE LAP BARS ARE DRAWN (2026-08-27)** — the harness bank is the same shape, and it stood here as "still not drawn" for one day. `AddRestraintBar` in `TrainMesh.h`: one bar per seat row (`FTrainSettings::RowsPerCar`, which the station now READS for its gate count, so gate g faces bar g), two arms and a crossbar, swung by `(1 − position) × BarRaisedDeg`. **THE HINGE IS AT THE FRONT, and the first one was backwards (fixed 2026-08-28 off a photograph)** — it pivoted behind the hip and swung forward over the thighs, so raised it leaned over the seat from behind; a real pivot is low and ahead of the rider. **AND ON THE THIRD TRY IT IS AT THE FLOOR, which is an ARRANGEMENT rather than a number** — behind the hip, then the front of the bay at thigh height, both swing arms reaching in over the lap from a pivot up in the air, so moving one only moved the guess. From the Big Thunder Mountain reference **the tube runs the whole way down to the cabin floor at the front edge of the seat, where the mechanical lock is**: the tube IS the locking member, and the bar stands nearly upright — closed leaning back over the lap 15°, open falling forward into the bay 45°. The rest derives from the SEAT (`BarHingeAheadM` is the squab's front edge; `BarArmLengthM` reaches the floor to `BarOverLapM` above the squab). Measured: closed 0.03 m under the rim, open 0.09 m below that, the tube within 0.022 m of the floor, 0.33 m of seat behind it. **TWO ASSERTIONS INVERTED AND BOTH HAD BEEN TRUE BY ACCIDENT** — *open is above the rim* belonged to the swing arm (a floor-hinged bar falls forward and DOWN, so the ANGLE is what reads); and *half way is higher than closed* was worse, since the bar passes through vertical and its height at 15° forward equals its height at 15° back. Half way is measured on the LEAN now, which is monotonic in the bank's position. **Not modelled:** a real BTM car's bar also blocks a side access lane — our tub is a bucket entered from above, and that is a car TYPE rather than a bar. **Closed it lies inside the shell, where a real one is; raised it stands over the rim** — the readable state is the one that means the train is not ready. A fifth train section because a fifth material; the angle moves vertices and never topology, so the actor updates in place. `SampleRestraints` now snapshots `GroupPosition` per group alongside the state and holds it away from a platform, and **a train never sampled draws CLOSED** — it is out on the course with riders, and a bar defaulting open there is a fault the ride does not have. Rows map onto groups by the gates' rule, so a jammed section raises the rows its gates serve. Asserted engine-free (closed, same topology at every angle, raising row 3 moves row 3 alone) and in `-TUSmokeTest`: 5 rows all raised on release, locking with group 0 jammed leaves 2 raised, 840 vertices in place. The old debug boxes (`bShowRestraints`) are off by default now and kept for their colour — a stuck group is red there and merely motionless on the mesh. **AND THE SHELL HAD A ROOF, so the first bars were lost in it** — closed into a sealed box, poking through the top when raised, reported from a screenshot the same evening. `CarBodySection` is a TUB now: over a rolled rim, down the inner wall, across a cabin floor set just above the shoulder so the cavity stays inside the full-width part of the shell; the caps are ear-clipped because a tub is non-convex and a centroid fan across a reflex corner is inside out. Two seats a row (a sixth section, upholstery), and **the bar's hinge derives from the squab** rather than an authored height. Asserted: open air on the centreline under the rim, shell in the wall, the closed bar over the seat and under the rim. **TWO ROWS A CAR AND A BULKHEAD AT EACH END (2026-08-28)**, both from the next screenshot: one row left half the shell empty, so `RowsPerCar` is 2 (spaced by `RowCentreX`, and the station's gate count follows); and the tub's caps close the shell's THICKNESS and nothing else, so the cabin was open front and back. `EndPanelThickM` closes it, let into the walls rather than butted (the seats' doubled-edge lesson), asserted as a body triangle inside the cabin at each end. **AND THE RIDER CAMERA SITS IN THE SEAT THE MESH DRAWS**, same day: it had one row a car from `Seat.h` and a 0.25 m knob over the heartline, so two rows put it between them at a child's eye height. `SeatOffsetAlongM` now takes the shell gap and is asserted equal to `RowCentreX` row by row; the eye is `RiderEyeAboveHeartline` (floor, squab, a seated adult), 0.20 m behind the row centre; the knob is gone; G is still measured at the heartline. **AND THE RIDER CAN TURN THEIR HEAD (2026-08-29)** — `FHeadLook` in `CameraRig.h`, composed **in the seat's frame** so it rolls with the car: a world-space yaw would leave the rider watching the horizon through an inversion while the car went round them, which is a drone shot rather than a ride. **Clamped both ways, never wrapped** — `FOrbitState` wraps yaw because an orbit really can go round and round, where a neck cannot and a wrap would leave somebody facing backwards with no idea how they got there (135° and 80°). **Recentred, not decayed**, because watching the drop fall away behind you is most of what the gesture is for; `[F]` recentres, through `PressFrameKey` rather than a branch inside `FrameWholeTrack`, which seven other callers want for the framing. In RIDE the mouse is captured and this is free look with nothing held; on the rider camera reached with `[C]` from Build or Operate the cursor is out for the panels, so it follows the same right-drag every other camera uses. It takes **free-fly's invert flags, not the orbit's**, because it is the same gesture — turning your head rather than dragging a subject — and that is precisely why those two settings were split.
- **Evacuation zones themselves are still not modelled** — the geometry exists now, but an evacuation ZONE is a place with an egress route off the ride, which is more than a deck.

What is actually worth starting on:

1. ~~A fast reference recording from NoLimits 2.~~ **DONE 2026-08-06, and both coefficients are now measured rather than derived.** A dead-flat straight launched to 142.5 km/h and coasted 1350 m: `DragK` = **0.000100** (the derived 0.00045 was 4.5× too high) and `Crr` = **0.026**, a 0.04% fit. Applied the same day; the only assertion that moved was the mid-course entry speed on the two-train circuit, 26.5 → 30.39 m/s. **Nothing geometric changed and every dynamic figure did** — before quoting any speed or G number from an older doc revision, check it against `PHASE0_FINDINGS.md` § "Validation against NoLimits 2".
**RESTRAINTS AND GATES ARE COMMANDED DEVICES** (`FCommandedBank`, one instance each), not a dwell timer: commanded position, travel time, and a count of separately-sensed **groups**, with locked meaning EVERY group reports locked. That is what makes "commanded closed but car 3 is not locked" expressible — the failure a walk-round exists to find, and one a single bool could not say. Travel time is HARDWARE, not crew allowance, so securing takes the longer of the walk-round and the bars. The crew OWNS the banks, so no caller of `Serve` changed. **The all-clear is now two contacts and a walk-round** — bars down, gates shut, and an operator who has looked — so any one of the three outstanding holds the dispatch, and a jammed gate holds it for ever rather than being timed out of the way.
**PRE-LAUNCH IS BUILT.** The permissive carries a third term: the device you are dispatched *into* must be **ready**, not merely clear — a launch armed, a chain turning. `FTrackDrives::IsReady` is `Commanded == Output && !faulted`; a drive still ramping is not ready, and those states were distinguishable for free the moment a command stopped taking effect instantly. **Plain track is always ready** — a term that denied a dispatch onto open course would stop the ride rather than protect it — and an E-stopped ride is ready for nothing. Ramps default to off, so every shipped preset satisfies it in one frame and nothing measured moved; give a launch a real ramp to see it.

**THE CONTROLLER IS A MACHINE NOW, not an implication** (`BlockSignal/PlcUnit.h`). `FRideSignals` plus `ServeHolds` is the PLC *program*; `FPlcUnit` is the PLC — the same gap the drives had before `FTrackDrives` and the sensors had before `FTrackSensors`, and closing it is what turned each of those from correct-looking into able to disagree with itself. It owns four real operational states the model could not express: a **key switch** (RUN / PROGRAM / STOPPED, where PROGRAM is the honest home for "maintenance is working on it"), a **watchdog** (the scan overrun the accumulator already detected becomes a latched FAULT rather than a logged note), a **program identity**, and **power**. **THE STANDARD PLC, NOT THE SAFETY CHAIN** — it may withhold permission to run and has no authority to prevent a stop, because the E-stop is inside `FTrackDrives` and the brakes are fail-safe, so neither routes through it. That is constraint 7 made structural, and `test_plcunit.cpp` asserts it against the real drive layer rather than by reading the code. `OutputsEnabled()` **permits, it does not compel**: false means the controller commands nothing, and a device with no command falls to its safe state exactly as it would with the cabinet unplugged. Two things fall out that were previously unimplementable. **Block state is not trusted across a power cycle** — the Tier 1 card asked for a course-clear walkdown and operator reset, which only means anything once something can be switched off. And **a program built for a different layout refuses to RUN**: the identity is a digest of the *derived blocks and zones* rather than the authored segments, so a geometry tweak that moves no boundary does not invalidate it — and it is the first detector this project has ever had for "I changed the code and the editor is still doing the old thing". Defaults are RUN, powered, walked, matching, so every measured figure is unmoved.

**THE GENERATED DEFAULT PROGRAM IS BUILT** (`BlockSignal/PlcProgram.h`). One POU per HOLDING device — a trim cannot start a train and a launch cannot stop one, so neither has a dispatch permissive to override. **The generated text IS the real permissive**, not an illustration: the same AND `ServeHolds` builds, with the station's three contacts and its walk-round spelled out rather than hidden behind one `ready` flag. **Every generated permissive is asserted to PARSE AND BIND in its own language** — a tutorial that does not compile teaches the wrong thing. **AN OVERRIDE IS PER BLOCK AND NEVER A MODE**, which is the NL2 lesson: their fatal choice was that scripted mode disables automatic block mode entirely, so customising one brake costs 500+ lines and the whole ride. **A broken override keeps the author's text, runs the generated default, and says so LOUDLY** — the third of three options, because rejecting the edit loses work, running an unparsed block loses a train, and a block silently running its default while its author believes otherwise is worse than either. **Regeneration never touches an override**, overrides are carried by NAME not index (an inserted device shifts every index and must move nobody's work), and **an orphaned override is REPORTED rather than eaten**.

**TIER 1'S WIRING IS BUILT** (`BlockSignal/SafetyIo.h`). `FTrackDrives` held the E-stop and its stop categories; this is everything BELOW that, which had been plain booleans. **DE-ENERGISE TO TRIP:** a safety input is a CIRCUIT carrying current while all is well, so a stop is demanded by the ABSENCE of current — a broken wire, a pulled connector, a dead supply and a pressed button are the same signal, and nothing has to know which. **Two channels of OPPOSITE polarity**, because one channel fails to danger in exactly one way (a short to the supply reads energised for ever, so a pressed E-stop does nothing) and doubling it does not help — the fault that makes one read safe makes the other read demanded. That is diversity rather than redundancy. **A stop is EITHER channel, not both**; a pair requiring agreement is a system where one broken wire disables the E-stop. The **discrepancy window** exists because two contact blocks never switch on the same millisecond — 50 ms of disagreement is normal, a second is a latched fault. **EXTERNAL DEVICE MONITORING closes the welded contactor**, which `FAULTS.md` had listed as caught by nothing: mirror contacts wired NC into the reset circuit, so the reset is a question the machine answers and a welded contactor can never be talked past. **The relay carries instantaneous AND delayed outputs, and Cat 1 is impossible without both** — contactors that open instantly leave nothing to ramp with. **A restraint fails the other way and that is not an exception**: fail-safe was never "de-energise", it is *fail to the state that cannot hurt anybody*. **Not modelled:** OSSD pulse testing, cross-relay monitoring, PL/SIL arithmetic — how an installation *proves* a category rather than implements one.

**The drive is a CiA 402 state machine** (`BlockSignal/Cia402.h`), and implementing it as specified IS the real thing rather than a model of one — IEC 61800-7-201/301 over EtherCAT as CoE is the actual interface behind Beckhoff, KEB, SEW and Elmo. Eight states, of which **only Operation enabled produces torque**, which is the whole reason a drive has eight rather than a bool. Four things carry the authenticity for almost no code and each is asserted: **the enable handshake cannot be skipped** — writing `0x000F` at a disabled drive does nothing for ever, because a PLC must write Shutdown, *wait for the statusword*, write Switch on, *wait*, then enable; **quick stop is ACTIVE LOW**, so a zeroed controlword — a dead master, a pulled cable — commands a stop rather than nothing, which is de-energise-to-trip expressed in a fieldbus word; **a fault reset needs a RISING EDGE**, so an operator holding it waits for ever, the same anti-tie-down shape as the monitored E-stop reset; and **a fault is not instant**, the drive running its configured reaction (605Eh) before it settles. The statusword is **derived from the state, never stored beside it** — a stored copy is a second truth. `FTrackDrives` owns one per drive and **drives it from decisions already made rather than in parallel**, so the two cannot disagree; the maintenance panel shows the statusword instead of the four states this project invented. The stop categories map straight on: **Cat 0 is STO** (voltage away, coast) and **Cat 1 is SS1** (quick stop ramp, then voltage away) — IEC 61800-5-2 and this project's own E-stop describe the same two paths.

**SHOW CUES: THREE TIME MODELS, AND THEY COMPOSE** (`BlockSignal/ShowCues.h`). **Timecode** (a master clock, restartable anywhere, tolerant of one subsystem dying), **relative offsets**, and **triggered chains** — the last being the glue between a NONDETERMINISTIC ride and DETERMINISTIC shows, and the reason a coaster's show system is not one long timeline. The ride is a triggered chain at the top; each scene is clock-driven for its 10-40 s life; idle and ambient loop and are **pre-empted by priority rather than mixed**, because two sequences driving one fixture is a fight. **FOLLOW IS NOT HANG**, and conflating them is the single most common real show-programming bug: time from the previous cue's START versus from its COMPLETION. Both are modelled separately and the difference is asserted the only way it shows — **edit a duration by 2 s and follow moves nothing downstream while hang moves everything**. **Drop-frame drops NUMBERS, not frames**, which everybody gets backwards: an hour of 29.97DF lands at 3599.996 s where the naive conversion is 3.6 s late. **A loop's period is the last cue's FOLLOW, not its duration** — the first version used duration and made a one-second loop fire every 100 ms, and the loop is asserted drift-free over ten minutes.

**THE EFFECT DEVICE LAYER IS BUILT** (`BlockSignal/DmxFixtures.h`). `FShowBus` decides what fires; this is what it fires INTO. DMX512 modelled **literally but INTERNALLY** — universes, 512 slots, a start code, fixtures at addresses — with **no network I/O, ever**: a sim that drove real fixtures would be a lighting console with a coaster attached. **DMX HAS NO FADES**, which is the fact people get wrong: a packet is absolute values and the console computes every intermediate one, so a full 513-slot packet is 22.7 ms and **44 Hz is the hard ceiling** (24 slots is 784 Hz — shorter packets go faster, which is why a small rig's strobe looks smoother). Addresses are **1-based twice over**, within the universe and within the fixture, and slot 0 is the start code and not a channel. **Overlapping fixtures are REFUSED**, never shuffled — the symptom is a light that flickers when a different one is cued and nobody traces it to addressing. **Data loss is selectable and both choices are defensible**: shutdown for haze, hold-last for architectural, and hold-last is WRONG for anything hazardous. **ARM PLUS LEVEL** is the characteristic shape of effect gear and exists so one stray channel cannot start an effect. **A DUTY CYCLE is enforced by the DEVICE, not the cue** — an operator holding a fader down does not get 30 s of flame, and putting that rule in the cue layer would let a bad cue destroy hardware. A **10 s igniter warm-up DROPS cues rather than queueing them**, because firing a flame effect ten seconds late is worse than not firing it. **Flame is not fired by the show controller at all** (NFPA 160: a safety-rated controller and a human-held positive enable sit between the request and the gas).

**The generated control panel is BUILT** (`ATUCoasterRide::DrawControlPanel`, `[P]` in play, debug canvas so it needs no asset). It walks the same block and zone lists the geometry and physics walk: a block becomes an indicator, a powered run a VFD module, a platform a sequence readout. Every number is read live — nothing is cached or computed for display, so it cannot drift. **Corrected against photographs of three real operator consoles**, and `SIGNALLING.md` § "What three real consoles taught it" records what they changed and the list of controls they have that this does not — do not add a lamp for one of those without modelling it first. Two things worth keeping: the block strip gives every block a **minimum width** (strict proportion made four 10 m platform positions three pixels on a 1288 m circuit), and there is no yellow lockout band because lockout is not modelled.

**THE RIDE ALWAYS HAS A CREW — `bPlayerIsCrew` (2026-08-08).** It was called `bAttended` for a few hours and that was wrong: *attended vs unattended* implies the ride can run with nobody there, which is the impression this model most needs not to give. There is always somebody on the platform; the only question is whether it is **you** or the simulated crew standing in for one — and `FAutoStationCrew` is a stand-in FOR A PERSON rather than an absence of one. The panel says which, in both cases, because a console that simply lacked the gate switches would read as a ride with no gates when what is true is that somebody else is working them. A person shuts the gates when the platform is clear, and a person confirms **by hand** that every harness is locked and immobile; there is no safety device that does either without somebody. **"Automatic" never meant the machine acts** — it means the machine PERMITS what is safe and refuses the rest, which is why the operator's controls sit beside DISPATCH rather than replacing it. **A loading that runs long because a rider needs help is the ordinary case, and it is exactly what a dwell timer cannot express.** `FCommandedBank` was always built for this — its own header says GATES and RESTRAINTS are CLOSE/OPEN selectors the operator commands with sensors confirming — and what was wrong is that `FAutoStationCrew` was pressing those switches on a clock, which that class has always called itself a stand-in for. With you as crew the banks still TICK (the hardware is real whether or not anybody is standing there — a bar takes its two seconds on a quiet Tuesday) and takes the contacts from the switch positions, each being what its own field comment already said: *gates shut* IS "everyone seated, airgates shut"; *bars up* is what lets riders out; and **the walk-round derives from nothing**, because it is a person having looked, so it has its own button. **OFF BY DEFAULT, and not as a judgement about which is right** — every measured figure in this project, the 5.5 s a rear-position delay costs and the 52 s a front one does and every capacity number, was taken with the crew running, and flipping the default would invalidate all of them silently. Taking over is opt-in until those are re-measured against a human. **The stand-in's weak point is that its dwell is a CONSTANT** — a platform running long is ordinary rather than exceptional — and riders, crowd capacity and the delays that come with them are their own piece of work; until then the dwell is authored and a delay is something you type.

**THE CONSOLE CAMERA, on `[C]` (2026-08-08).** Standing at the platform is where operators work and where everybody wants to stand. It **gives up the view Operate's orbit was chosen for** — far enough back to see the train AND the block ahead — and that is the right trade rather than a regression: a real operator cannot see the mid-course brake either, they read it off the block schematic, which the panel already draws. The difference is that the schematic stops being a nicety and becomes the instrument. Orbit is one press away. The stand position is **derived from the first platform the block walk already found**, a third of the way in, 2.5 m to the rider's left and 1.6 m above the heartline — so it is right on a layout nobody has built yet.

**ONE OPERATOR, ONE POSITION (2026-08-08).** The controls commanded EVERY platform at once — shutting the gates shut them at four places somebody cannot see, and a walk-round given at one position was recorded against all of them. Correct and invisible on a single-platform ride; wrong on the small-batch preset, which has an unload position and three loading ones. `ConsolePlatformPtr()` is now **one answer shared by the draw and by every command**, because the panel picking one platform to display while the buttons commanded all of them is exactly how it happened. The header is the **position selector** and says whether it is pinned or following the train, and `bOperatorAllClear` moved onto `FTUPlatform` — *"I have looked at this one"* is not a claim about the two in front of it. Dispatch follows the console too, and only bites in MANUAL, so every measured figure (all taken in AUTO) is unmoved.

**THE CONSOLE IS PRESSABLE (2026-08-08).** It carried a comment saying *"a screen cannot be pressed, so these are indicators rather than buttons"* — which the menu, the segment editor and the diagnostics list had already disproved three times over. Four controls: **DISPATCH**, the **AUTO/MANUAL** selector, **E-STOP** and **RESET/ACKNOWLEDGE**. **A MOUSE HAS BOTH EDGES, and that is the part that matters** — dispatch is anti-tie-down so holding it must not send train after train, and the E-stop reset is MONITORED and fires on the RELEASE; press-to-hold and let-go-to-release model both exactly, so a click is the same signal the key sends rather than a weaker stand-in. A held button **draws as held**, because a control that looks identical pressed and released is one nobody can tell they are still holding. The release is taken **unconditionally on mouse-up**, since a dispatch left held because the pointer wandered off the control is a wedged button — the exact failure anti-tie-down exists for. **The controls are a SECOND ROW rather than the lamps made clickable**: on a real console indicators and controls are different objects in different places, and a lamp you can press is one somebody presses while pointing at what it says. **What stays an indicator is deliberate** — harness, gates and dispatch-ready report a state the crew and the interlocking own, and making those pressable would move an authority rather than wire a button.

**It has TWO VIEWS, and the split is a real one rather than a detail level** — `ETUPanelView`, `[P]` cycles operator → maintenance → off. Every installation has both: an operator dispatches trains, a maintainer diagnoses machines, and **motor current belongs to exactly one of them**. So the drives section is the one place the views genuinely disagree — the operator gets one state word per drive (`RUNNING` / `RAMPING` / `FAULT` / `STOPPED`, all facts a drive holds about *itself*), and maintenance gets commanded-vs-output-vs-motor, torque, and fault acknowledgement. Maintenance also gets the **second detection method's own per-block counts** under the schematic, plus a `DETECTION` line that states the agreement — because a cross-check never *seen* to agree is indistinguishable from one that is not running. **Not a difficulty setting**: nothing is dumbed down and nothing is invented, the same live reads are asked two different questions. `Docs/UI_CONVENTIONS.md` records why a "reskin"/mod layer was rejected in favour of this.

**Where a held train parks is settled, it is not the middle, and A SWITCH SAYS SO.** Each zone carries a **stop mark** at `deviceEnd − HoldNoseClearanceM` (1 m) and the dispatcher's whole rule is *truck forward until the mark trips* — no train length, no arc length, no zone extent, because a PLC has none of those three. Measured from the end and applied to the nose, because the margin exists to stop a train protruding into the next zone through a defect. Train length is consumed **once, where the mark is placed**; putting it back into the dispatcher is undoing the point. The brake does not put the train there — it stops it well short and the drive tyres truck it the rest of the way, up to 67.3 m on the mid-course brake. Measured at all five devices: mark 1.00 m short, nose parks 0.18 m past it, **0.81 m clear**, identical everywhere because the overshoot is `v²/2a` at the crawl speed and grip. Changing any of this is changing a safety margin: read the holding section of `SIGNALLING.md` first.

**The scan runs at a FIXED PERIOD, not once per rendered frame** (`ATUCoasterRide::SimStep`, `SimHz` = 240, an accumulator in `Tick`). It used to run at frame rate, which was wrong twice: **unfaithful**, because a PLC scans on a fixed period and does not go faster when the graphics card is idle — every rate in the control system moved with the frame rate, and the documented "a train can cross a sensor inside one scan" limit was quietly *worse* on a slow machine; and **not reproducible**, so nothing could be recorded and replayed and no fault-injection scenario could prove anything. On a backlog past `MaxStepsPerFrame` the time is **DROPPED, never worked off** — catching up means running the ride faster than real time, and a ride that fast-forwards through a hitch can skip a train past a block boundary. Reported as a scan overrun, which is what a real controller does with a missed deadline. This also makes the actor and `test_twotrains.cpp` step at the same rate, so "line-for-line stand-in" is true rather than aspirational.

**Trains run real laps, and `bCircuit` is MEASURED, never authored.** The actor checks position, heading and roll at the seam on every rebuild and only wraps a layout that passes; the other three presets still return to the station by teleport, which is correct for track whose two ends are hundreds of metres apart. Read the lap entry in `PHASE0_FINDINGS.md` before touching it — `S -= TotalLength` is one of four changes, and the other three (distance travelled, the sample walk across the seam, and what a reversed rear/front pair *means*) are each a silent failure if missed.

**A circuit closes by SHAPE, not by solver, and the shape is load-bearing.** An oval closes analytically when both turns are the same way, exactly 180°, same radius, same easement; the only condition left is that the return leg's horizontal extent equals the two collinear outbound legs'. That condition means **every metre spent outbound is paid for twice**, which is why the turnaround sits at the top of the hill rather than at each end — measured, the obvious layout came to 1717 m and stalled in every variant. See `PHASE0_FINDINGS.md`.

Before quoting any number from this project, check whether something already measures it — `reference_figures.cpp` for the reference layout, `test_twotrains.cpp` for the closed circuit (it prints its canonical figures on the way out), the assert suites for the models, `PHASE0_FINDINGS.md` for what has already been disproved. Several plausible fixes on that page were measured and rejected, and re-proposing one without saying why yours differs wastes everybody's time.

## Phase 0 — Prototype (complete)

Kept because the *reasoning* is still binding, not because the work is outstanding. Every item below is done; the notes attached to them are decisions that still hold.

1. ~~Evaluate Coaster Forge (Fab/Dualstate Games) — a build-vs-adapt decision for the procedural track meshing layer.~~ **DECIDED: build.** Coaster Forge is a commercial product and cannot be redistributed under MIT, so "adapt" was never available — constraint 3 below already said as much. Buying it would have bought reference reading, not a shortcut. The one pattern the plan wanted from it, zone-based speed control, was arrived at independently and verified against NoLimits 2 (`FTrackZone` in `Prototypes/TrainPhysics/`). Phase 4 meshing is written from scratch against `Prototypes/TrackSpline/TrackProfile.h`. **This closes the last Phase 0 gate.**
2. ~~Prototype the curvature-continuous spline math standalone.~~ **DONE** — `Prototypes/TrackSpline/`. Note the representation is **curvature-profile-over-arc-length**, not the cubic Hermite/B-spline model the plan originally called for: a segment carries curvature varying linearly over arc length (straight `k=0`, arc `k=const`, clothoid `k` linear) and geometry comes from integrating a moving orthonormal frame. C² continuity is therefore a property of the data, not something fitted. This supersedes the earlier wording — **do not "restore" a Hermite/control-point formulation.**
3. ~~Prototype the block-occupancy + buffer state machine as a plain C++ class with unit tests.~~ **DONE** — `Prototypes/BlockSignal/`.
4. ~~Vertical slice: a single hand-authored spline, a cart that follows it with real physics, a basic camera.~~ **DONE** — `Source/TrackUnlimited/`. Use `FTrackFrame::Tangent` for direction; never finite-difference `EvaluateAt`, and treat it as O(track length) per call. That warning applies to anything differentiating the geometry, including the closure solver, which is why its finite-difference step is deliberately coarse.

Added beyond the original plan: `Prototypes/TrainPhysics/` — the Section 5 energy-based motion model, built standalone because the vertical slice needs it. Gravity is an exact energy exchange, not an integrated force; powered sections are tractive accelerations that pass through the same energy accounting. **Do not "simplify" a zone back into a post-hoc clamp on speed** — that formulation manufactures energy on a gradient, which is measured and recorded in `Docs/PHASE0_FINDINGS.md`.

`Prototypes/BlockSignal/BlockSignal.h`, `Prototypes/TrackSpline/TrackSpline.h` and `Prototypes/TrainPhysics/TrainPhysics.h` are the **canonical designs to port** into UE5 C++ — not references to reimplement from scratch. `Docs/PHASE0_FINDINGS.md` has a measured port checklist.

Also added: `Prototypes/NL2Csv/` — reads and writes NoLimits 2's documented tab-separated spline export, so real NL2 layouts can be driven through the model and our tracks opened in NL2 to compare G and speed traces. This is a **validation and test-fixture path, not an authoring path**, and it does not soften constraint #1 above: an imported track is thousands of derived micro-segments with the original segment vocabulary unrecoverable, so it is not something anyone edits. Do not let it grow into a back door around parametric authoring, and do not commit NL2 park files or exports of real rides (`Prototypes/NL2Csv/Tracks/` is gitignored for that reason).

One rule from this phase that outlived it, because it has now bitten twice: **these headers are engine-free but they are compiled INTO the engine, so a type name only needs to be unique against all of UE.** `FFrame` collided with the Blueprint VM's and became `FTrackFrame`; `FField` collided with `UObject/Field.h`'s and became `FTrackIOField`. Standalone clang compiles both cleanly, so the collision only appears at the port. Check a new type name against the engine before, not after.

## Key vocabulary (used throughout the docs and should be used consistently in code/comments)

`Docs/GLOSSARY.md` explains these and the geometry terms (curvature profile, clothoid, parallel transport, holonomy, torsion, Darboux vector, felt G) in terms of what they are on a real coaster. The list below is the short form for code consistency; the glossary is for understanding what the words mean.

- **Block buffer / overlap** — the safety-margin state a block holds after a train physically exits it, before it reports CLEAR. Named after the real-railway "overlap" signaling concept.
- **Heartline** — the reference line (not the rail centerline) that banking and ride-camera calculations are computed around, so felt-G through banked turns is physically correct.
- **Roll vs bank** — not synonyms, and the difference is a per-segment mode (`ERollMode`). **Roll** is measured from the rotation-minimising path frame: defined everywhere including inverted and vertical track, and what the integrator sees. **Bank** is measured from the horizon — what a spirit level reads — and is undefined pointing straight up. Say which one you mean; `Roll = 0` is not level on non-planar track.
- **Dispatch permissive** — the logic gate that allows a station/launch to release a train, based on downstream block clearance (and, for high-speed sections, braking-distance lookahead).
- **PLC vs VFD** — not interchangeable, and it is an easy slip. The **PLC** is the brain: reads sensors, holds block state, evaluates permissives, commands devices on a scan cycle. A **VFD** is the muscle for *one* motor: it takes a speed command and drives a tyre or chain to it. There is one PLC and many VFDs. **`FRideSignals` plus `ServeHolds` IS the PLC program** — that is the right way to think about what has been built. `Docs/CONTROL_ARCHITECTURE.md` has the tier split and the IEC 61131-3 grounding.
- **VFD module** — the generated control-panel element for one drive on a powered segment (lift chain, tyre launch): target frequency/speed, actual motor feedback, torque/current draw, ramp rate.
- **Block brake vs trim brake** — a block brake is a friction pad *and* drive tyres: the pad trips on entry and stops the train as hard as rider comfort allows, then the tyres convey it to a holding position. A trim can only slow a train, so a train parked on one stays there. Only block brakes bound a block or count toward capacity.

## Conventions in prototype code

The prototypes are metres, radians, seconds, and a **right-handed** frame where `Tangent × Lateral = Up` and `+Lateral` is the rider's **left**. UE5 is centimetres and **left-handed** with `+Y` to the right.

Convert units *and* flip handedness at the port boundary, never inside the math. The flip is not a single sign: UE's `Right` is `-M(Lateral)` where `M(x,y,z) = (x,-y,z)`. Getting this wrong mirrors the entire track and produces geometry that still looks self-consistent — `Docs/PHASE0_FINDINGS.md` has the measured residuals.

**And M is a REFLECTION, so it reverses triangle winding — BUT DO NOT SWAP INDICES AT THE PORT. CORRECTED 2026-08-09, after months of every surface being inside out.** Determinant −1, so the flip genuinely does reverse orientation, and `test_trackmesh.cpp` still asserts exactly that. The wrong step was the conclusion: that is a statement about GEOMETRY, and **UE's front-face rule is the opposite handedness and flips it back**. Two flips. Doing one explicitly is what broke it. **It hid because every assertion in this project stops at the port** — signed volume, watertightness and normals-agree-with-winding all passed, because the engine-free mesh was never what was wrong — and because **a thin tube inside out has the same silhouette**, so 1288 m of rail looked plausible. It showed the day something SOLID was drawn: a camera placed INSIDE a support pier saw walls, which outward-wound geometry cannot do. Settled by looking, both ways, rather than by argument.

## When in doubt

This is a solo-developer, free/open-source project with no funding cushion — favor small, shippable, testable increments over broad refactors. If an implementation choice isn't covered by `Docs/PROJECT_PLAN.md` or this file, flag it and ask rather than assuming; the architectural decisions recorded here were made deliberately, through real back-and-forth, and getting them right mattered enough to write down.
